# Quick Task 260402-k3n: fix failing TestScenarioAudienceSplit in tests/test_ai_contract.py

## Scope

Fix the single failing AI contract test so it matches the current runtime/session preconditions introduced by E93.

## Tasks

1. Update the scenario fixture in `tests/test_ai_contract.py` to create/select a profile and advance to a phase where message handling is valid.
2. Keep the assertion focused on a stable public output that the current command flow guarantees.
3. Re-run the targeted test, then the project verification gate: `uv run pytest -q` and `uv run python -m dm_bot.main smoke-check`.
