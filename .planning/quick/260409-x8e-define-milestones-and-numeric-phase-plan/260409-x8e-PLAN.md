# Quick Plan — `260409-x8e`

## Task

Define milestones and numeric phase plans for all four workstreams: `track-runtime`, `track-identity`, `track-surface`, and `track-ops`.

## Why This Task Exists

Planning is currently uneven:

- `track-runtime` is phase-planned, but some roadmap language still uses slug-like phase identifiers
- `track-identity` has milestone definitions plus a detailed `v1.0` plan, but `v1.1` is not yet fully folded into execution-facing state
- `track-surface` and `track-ops` still sit at fresh-baseline placeholders without milestone and phase stacks
- root planning state still implies only part of the 4-track system is ready for execution

The goal is to remove planning ambiguity so later work can be execution-led instead of roadmap-surgery-led.

## Planned Changes

1. Normalize phase references to numeric `Phase 01 / Phase 02 / ...` style.
2. Complete `track-runtime` and `track-identity` planning state so both are clearly execution-ready.
3. Define milestone stacks, requirements, and consolidated phase plans for `track-surface` and `track-ops`.
4. Sync root planning docs so the repository reflects that all four workstreams are planned.
