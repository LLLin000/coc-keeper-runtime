# Quick Plan — Planning Reset Archive `pre-reset-2026-04-02`

## Goal

Archive the current multi-track planning tree into a clean snapshot and rebuild `.planning/` as a fresh 4-track baseline with less overlap and clearer canonical ownership.

## Scope

1. Create a reset archive root for the old planning cycle.
2. Lightly organize obvious legacy/duplicate planning areas before archiving.
3. Rebuild root planning truth files for the new cycle.
4. Replace the old 5-track layout with a new 4-track workstream skeleton:
   - `track-runtime`
   - `track-identity`
   - `track-surface`
   - `track-ops`

## Deliverables

- Archive manifest under `.planning/archives/pre-reset-2026-04-02/`
- New root `PROJECT.md`, `ROADMAP.md`, `STATE.md`, `MILESTONES.md`, `active-workstream`
- New workstream skeletons under `.planning/workstreams/`
- Old planning execution artifacts moved out of the active root

## Verification

- `uv run pytest -q`
- `uv run python -m dm_bot.main smoke-check`
