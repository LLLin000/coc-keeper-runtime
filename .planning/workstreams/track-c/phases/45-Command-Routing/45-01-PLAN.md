---
phase: 45-Command-Routing
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - src/dm_bot/discord_bot/commands.py
autonomous: true
requirements:
  - CHAN-04
  - GUIDE-01
  - GUIDE-02

must_haves:
  truths:
    - Wrong-channel commands show redirect message to correct channel
    - Command help includes channel recommendation
    - Contextual guidance based on current channel
  artifacts:
    - path: src/dm_bot/discord_bot/commands.py
      updates: ChannelEnforcer integration
---

<objective>
Implement channel-aware routing with clear redirect messages for wrong-channel usage.

Purpose: Wire the ChannelEnforcer into BotCommands to enforce channel policies at runtime.
Output: Commands check channel type and show redirect messages when used in wrong channel.
</objective>

<context>
@C:/Users/Lin/Documents/Playground/src/dm_bot/discord_bot/commands.py
@C:/Users/Lin/Documents/Playground/src/dm_bot/discord_bot/channel_enforcer.py
@C:/Users/Lin/Documents/Playground/src/dm_bot/orchestrator/session_store.py

# Existing redirect patterns in commands.py:
- lines 538-544: redirect for /sheet command
- lines 746-752: admin channel guidance

# ChannelEnforcer provides:
- channel_type_for(guild_id, channel_id) -> ChannelType
- check_command(command_name, guild_id, channel_id) -> (allowed, message)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ChannelEnforcer into BotCommands</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    1. Import ChannelEnforcer at top of commands.py
    2. Add ChannelEnforcer instance to BotCommands.__init__:
       - Accept session_store parameter
       - Create self._enforcer = ChannelEnforcer(session_store)
    3. Add channel_check decorator or helper method:
       - def check_channel(self, command_name: str, interaction) -> tuple[bool, str]
       - Uses interaction.guild_id and interaction.channel_id
    4. Apply channel checks to archive commands:
       - /sheet - check in channel_check for "sheet"
       - /profiles - check in channel_check for "profiles"
       - /profile_detail - check in channel_check for "profile_detail"
       - /start_builder - check in channel_check for "start_builder"
       - /builder_reply - check in channel_check for "builder_reply"
    5. Apply channel checks to admin commands:
       - /admin_profiles - check in channel_check for "admin_profiles"
    6. Apply channel checks to game commands:
       - /take_turn - check in channel_check for "take_turn"
       - /ready - check in channel_check for "ready"
       - /load_adventure - check in channel_check for "load_adventure"
    7. When check fails:
       - Send ephemeral message with redirect_message
       - Return early without executing command

    Pattern for each command:
    ```python
    async def sheet_command(self, interaction: Interaction):
        allowed, msg = self.check_channel("sheet", interaction)
        if not allowed:
            await interaction.response.send_message(msg, ephemeral=True)
            return
        # ... original command logic
    ```
  </action>
  <verify>
    <manual>Test commands in wrong channels - should show redirect message</manual>
  </verify>
  <done>Commands enforce channel policies with redirect messages</done>
</task>

<task type="auto">
  <name>Task 2: Add command help text with channel guidance</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    Update command descriptions to include channel guidance:
    - /sheet: "查看角色档案 (仅限 #角色档案 频道)"
    - /profiles: "列出所有角色档案 (仅限 #角色档案 频道)"
    - /start_builder: "开始创建新角色 (仅限 #角色档案 频道)"
    - /admin_profiles: "管理所有玩家角色 (仅限 #管理员 频道)"
    - /take_turn: "进行游戏回合 (仅限 #游戏大厅 频道)"
    - /load_adventure: "加载模组 (仅限 #游戏大厅 频道)"
  </action>
  <verify>
    <manual>Check /help shows channel guidance in descriptions</manual>
  </verify>
  <done>Help text includes channel recommendations</done>
</task>

<task type="auto">
  <name>Task 3: Add contextual guidance based on current channel</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    Add channel-aware help that shows what commands are available in current channel:
    - When user runs /help in archive channel, show only archive commands
    - When user runs /help in game channel, show only game commands
    - When user runs /help in admin channel, show all commands (admin sees all)

    Implementation:
    - Modify help response to filter by channel type
    - Use ChannelEnforcer.channel_type_for() to detect current channel
    - Filter command list based on channel type
  </action>
  <verify>
    <manual>Run /help in different channels - should show contextual command list</manual>
  </verify>
  <done>Contextual guidance shows available commands per channel</done>
</task>

</tasks>

<verification>
- [ ] Archive commands redirect to archive channel when used elsewhere
- [ ] Admin commands redirect to admin channel when used elsewhere
- [ ] Game commands redirect to game channel when used elsewhere
- [ ] Help text includes channel recommendations
- [ ] /help shows contextual command list based on current channel
</verification>

<success_criteria>
1. Wrong-channel commands show redirect message to correct channel
2. Command help includes channel recommendation
3. Contextual guidance based on current channel
4. CHAN-04, GUIDE-01, GUIDE-02 requirements are implemented
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-c/phases/45-Command-Routing/45-01-SUMMARY.md`
</output>
