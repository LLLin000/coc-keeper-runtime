# Plan 43-01: Reliable Bootstrap And Operator Workflow

## Goal

Make restart/bootstrap behavior reliable enough that operators can trust panel and CLI status without repeated Discord trial-and-error.

## Delivered

- unified marker-based startup checks
- stable detached launch helper for Windows
- verified `smoke-check` end-to-end
- verified `restart-system` end-to-end
- verified panel state/action wiring against the same control layer

## Verification

- `uv run pytest -q`
- `uv run python -m dm_bot.main smoke-check`
- `uv run python -m dm_bot.main restart-system`
- `uv run python -m dm_bot.main control-status`
