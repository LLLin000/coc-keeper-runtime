---
phase: 66-model-router
plan: "01"
type: tdd
wave: 1
depends_on: ["60-test-infrastructure/60-01"]
files_modified:
  - tests/test_router_service_flow.py
  - tests/test_message_buffer_flow.py
  - tests/test_intent_classification_flow.py
autonomous: true
requirements:
  - ROUTER-01
  - ROUTER-02
  - ROUTER-03
must_haves:
  truths:
    - "RouterService.route() returns TurnPlan with valid mode, tool_calls, state_intents, narration_brief"
    - "MessageBuffer buffers during SCENE_ROUND_RESOLVING, releases on phase transition"
    - "RouterService handles invalid JSON gracefully (raises RouterError)"
    - "MessageIntent classification correctly maps content to intent"
  artifacts:
    - path: tests/test_router_service_flow.py
      provides: ROUTER-01 router service TurnPlan generation tests
      min_lines: 80
    - path: tests/test_message_buffer_flow.py
      provides: ROUTER-02 message buffering during high-intensity phases
      min_lines: 60
    - path: tests/test_intent_classification_flow.py
      provides: ROUTER-03 intent classification flow tests
      min_lines: 60
  key_links:
    - from: test_router_service_flow.py
      to: router/service.py
      via: RouterService.route() → TurnPlan
    - from: test_message_buffer_flow.py
      to: router/message_buffer.py
      via: MessageBuffer.buffer_message() / release_buffered_messages()
    - from: test_intent_classification_flow.py
      to: router/intent.py
      via: MessageIntent classification and should_buffer_intent()
---

<objective>
Validate intent classification, turn plan generation, and message buffering for single and multi-user scenarios.

Purpose: Close the "no router flow tests" gap. Write failing tests first (RED), then implement fixes (GREEN), then refactor (REFACTOR).

Output: 3 test files covering ROUTER-01 (TurnPlan generation), ROUTER-02 (message buffering), ROUTER-03 (intent classification).
</objective>

<execution_context>
@C:/Users/Lin/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/dm_bot/router/service.py
@src/dm_bot/router/contracts.py
@src/dm_bot/router/message_buffer.py
@src/dm_bot/router/intent.py
@src/dm_bot/router/intent_classifier.py
@tests/test_narration_service.py
</context>

<feature>
  <name>TurnPlan Generation (ROUTER-01)</name>
  <files>tests/test_router_service_flow.py</files>
  <behavior>
    - Test: route() returns TurnPlan with mode in {dm, scene, combat}
    - Test: route() returns non-empty tool_calls list
    - Test: route() returns non-empty narration_brief string
    - Test: route() with invalid JSON raises RouterError
    - Test: route() normalizes string tool_calls to dict format
    - Test: route() populates speaker_hints from model response
  </behavior>
  <implementation>
    Use AsyncMock for router client. Return deterministic TurnPlan-like dict.
    Test error handling for JSON decode failure and ValidationError.
  </implementation>
</feature>

<feature>
  <name>Message Buffering (ROUTER-02)</name>
  <files>tests/test_message_buffer_flow.py</files>
  <behavior>
    - Test: buffer_message() during SCENE_ROUND_RESOLVING returns True
    - Test: buffer_message() during COMBAT returns True for non-PLAYER_ACTION intents
    - Test: get_buffered_messages() returns all buffered for channel
    - Test: release_buffered_messages() clears buffer and returns messages
    - Test: clear_buffer() removes without returning
    - Test: has_buffered_messages() returns True when messages exist
    - Test: format_buffered_message_summary() returns formatted string
  </behavior>
  <implementation>
    Test MessageBuffer directly with MessageIntent values.
    Verify should_buffer_intent() gating from router/intent.py.
  </implementation>
</feature>

<feature>
  <name>Intent Classification (ROUTER-03)</name>
  <files>tests/test_intent_classification_flow.py</files>
  <behavior>
    - Test: OOC message → MessageIntent.OOC
    - Test: in-character social message → MessageIntent.SOCIAL_IC
    - Test: game action phrase → MessageIntent.PLAYER_ACTION
    - Test: rules question → MessageIntent.RULES_QUERY
    - Test: should_buffer_intent() True for PLAYER_ACTION during SCENE_ROUND_RESOLVING
    - Test: should_buffer_intent() False for ADMIN_ACTION during SCENE_ROUND_RESOLVING
  </behavior>
  <implementation>
    Test should_buffer_intent() directly with MessageIntent and session_phase.
    Test get_intent_priority() for all phases.
  </implementation>
</feature>

## Task Dependency Graph

| Task | Depends On | Reason |
|------|------------|--------|
| Task 1: RouterService TurnPlan generation (ROUTER-01) | None | RED phase - foundational |
| Task 2: MessageBuffer flow tests (ROUTER-02) | None | RED phase - independent |
| Task 3: Intent classification flow (ROUTER-03) | None | RED phase - independent |

## Parallel Execution Graph

Wave 1 (Start immediately - no dependencies on E60):
├── Task 1: RouterService TurnPlan generation tests (ROUTER-01)
├── Task 2: MessageBuffer flow tests (ROUTER-02)
└── Task 3: Intent classification flow tests (ROUTER-03)

Wave 2 (After Wave 1 - if fixes needed):
└── Any router fixes identified from failing tests

## Commit Strategy

### Commit 1: RED - Router service TurnPlan tests (ROUTER-01)
```
test(router-66): add failing router service TurnPlan generation tests (ROUTER-01)

- test_route_returns_valid_turnplan
- test_route_handles_invalid_json
- test_route_normalizes_string_tool_calls
```

### Commit 2: RED - MessageBuffer flow tests (ROUTER-02)
```
test(router-66): add failing message buffering flow tests (ROUTER-02)

- test_buffer_message_during_scene_round_resolving
- test_release_buffered_messages_clears
- test_format_buffered_message_summary
```

### Commit 3: RED - Intent classification flow tests (ROUTER-03)
```
test(router-66): add failing intent classification flow tests (ROUTER-03)

- test_should_buffer_intent_scene_round_resolving
- test_should_buffer_intent_admin_action_not_buffered
- test_get_intent_priority_by_phase
```

### Commit 4: GREEN - Verify all tests pass
```
feat(router-66): router flow tests passing - ROUTER-01/02/03 complete
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: RouterService TurnPlan generation tests (ROUTER-01)</name>
  <files>tests/test_router_service_flow.py</files>
  <behavior>
    - test_route_returns_valid_turnplan - TurnPlan has valid mode, tool_calls, narration_brief
    - test_route_handles_invalid_json - bad JSON raises RouterError
    - test_route_handles_validation_error - missing required fields raises RouterError
    - test_route_normalizes_string_tool_calls - ["roll_dice"] → [{"name":"roll_dice","arguments":{}}]
    - test_route_returns_speaker_hints - speaker_hints list populated
    - test_route_passes_envelope_fields - trace_id, campaign_id, channel_id, user_id passed to model
  </behavior>
  <action>
    Create tests/test_router_service_flow.py:

    ```python
    """ROUTER-01: RouterService TurnPlan generation tests."""
    import pytest
    import json
    from unittest.mock import AsyncMock, MagicMock
    from dm_bot.models.schemas import ModelResponse, ModelRequest
    from dm_bot.router.service import RouterService, RouterError
    from dm_bot.router.intent import MessageIntent
    from dm_bot.router.contracts import TurnPlan
    from dm_bot.models.schemas import TurnEnvelope

    @pytest.fixture
    def mock_router_client():
        client = MagicMock()
        client.call_router = AsyncMock(return_value=ModelResponse(
            content=json.dumps({
                "mode": "scene",
                "tool_calls": [{"name": "coc_skill_check", "arguments": {"label": "图书馆使用", "value": 60}}],
                "state_intents": [{"kind": "san_damage", "payload": {"amount": 1}}],
                "narration_brief": "调查员在书架间翻找线索",
                "speaker_hints": ["张记者"],
            }),
            finish_reason="stop",
            model="test",
        ))
        return client

    @pytest.fixture
    def router_service(mock_router_client):
        return RouterService(client=mock_router_client)

    @pytest.fixture
    def envelope():
        return TurnEnvelope(
            trace_id="t1", campaign_id="c1", channel_id="ch1",
            user_id="u1", content="我检查一下书架",
        )

    @pytest.mark.asyncio
    async def test_route_returns_valid_turnplan(router_service, envelope):
        result = await router_service.route(envelope, session_phase="scene_round_open")
        assert result.mode in ("dm", "scene", "combat")
        assert isinstance(result.tool_calls, list)
        assert len(result.narration_brief) > 0

    @pytest.mark.asyncio
    async def test_route_handles_invalid_json(router_service, envelope):
        router_service._client.call_router = AsyncMock(return_value=ModelResponse(
            content="not valid json {",
            finish_reason="stop", model="test",
        ))
        with pytest.raises(RouterError):
            await router_service.route(envelope)

    @pytest.mark.asyncio
    async def test_route_handles_validation_error(router_service, envelope):
        router_service._client.call_router = AsyncMock(return_value=ModelResponse(
            content=json.dumps({"mode": "invalid_mode"}),  # missing narration_brief
            finish_reason="stop", model="test",
        ))
        with pytest.raises(RouterError):
            await router_service.route(envelope)

    @pytest.mark.asyncio
    async def test_route_normalizes_string_tool_calls(router_service, envelope):
        router_service._client.call_router = AsyncMock(return_value=ModelResponse(
            content=json.dumps({
                "mode": "scene",
                "tool_calls": ["roll_dice", {"name": "check_san", "arguments": {}}],
                "state_intents": [],
                "narration_brief": "test narration",
                "speaker_hints": [],
            }),
            finish_reason="stop", model="test",
        ))
        result = await router_service.route(envelope)
        assert len(result.tool_calls) == 2
        assert result.tool_calls[0].name == "roll_dice"
        assert result.tool_calls[0].arguments == {}

    @pytest.mark.asyncio
    async def test_route_returns_speaker_hints(router_service, envelope):
        result = await router_service.route(envelope)
        assert "张记者" in result.speaker_hints or len(result.speaker_hints) >= 0
    ```

    Use AsyncMock for async route() method.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_router_service_flow.py -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>RouterService.route() returns valid TurnPlan; invalid JSON raises RouterError</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessageBuffer flow tests (ROUTER-02)</name>
  <files>tests/test_message_buffer_flow.py</files>
  <behavior>
    - test_buffer_message_returns_true - buffering succeeds
    - test_get_buffered_messages_all_for_channel - all messages retrievable
    - test_release_buffered_messages_clears_buffer - release + clear atomically
    - test_clear_buffer_without_return - clear_buffer removes without returning
    - test_has_buffered_messages_true_when_present - correct boolean state
    - test_format_buffered_message_summary - formatted output string
    - test_buffer_multiple_users - multiple users buffered separately
  </behavior>
  <action>
    Create tests/test_message_buffer_flow.py:

    ```python
    """ROUTER-02: MessageBuffer flow tests."""
    import pytest
    from dm_bot.router.message_buffer import MessageBuffer, BufferedMessage
    from dm_bot.router.intent import MessageIntent

    @pytest.fixture
    def buffer():
        return MessageBuffer()

    @pytest.fixture
    def metadata():
        from dm_bot.router.intent import MessageIntentMetadata
        return MessageIntentMetadata(
            intent=MessageIntent.PLAYER_ACTION,
            classification_reasoning="looks like a game action",
            handling_decision="buffered",
            was_buffered=True,
            phase_at_classification="scene_round_resolving",
        )

    def test_buffer_message_returns_true(buffer, metadata):
        result = buffer.buffer_message("ch1", "u1", "我检查书架", MessageIntent.PLAYER_ACTION, metadata)
        assert result is True

    def test_get_buffered_messages_all_for_channel(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "message 1", MessageIntent.PLAYER_ACTION, metadata)
        buffer.buffer_message("ch1", "u2", "message 2", MessageIntent.PLAYER_ACTION, metadata)
        msgs = buffer.get_buffered_messages("ch1")
        assert len(msgs) == 2

    def test_release_buffered_messages_clears_buffer(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "message", MessageIntent.PLAYER_ACTION, metadata)
        msgs = buffer.release_buffered_messages("ch1")
        assert len(msgs) == 1
        assert buffer.has_buffered_messages("ch1") is False

    def test_clear_buffer_without_return(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "message", MessageIntent.PLAYER_ACTION, metadata)
        buffer.clear_buffer("ch1")
        assert buffer.has_buffered_messages("ch1") is False

    def test_has_buffered_messages_true_when_present(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "message", MessageIntent.PLAYER_ACTION, metadata)
        assert buffer.has_buffered_messages("ch1") is True

    def test_buffer_multiple_users(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "Alice message", MessageIntent.PLAYER_ACTION, metadata)
        buffer.buffer_message("ch1", "u2", "Bob message", MessageIntent.PLAYER_ACTION, metadata)
        buffer.buffer_message("ch2", "u3", "Other channel", MessageIntent.PLAYER_ACTION, metadata)
        assert len(buffer.get_buffered_messages("ch1")) == 2
        assert len(buffer.get_buffered_messages("ch2")) == 1

    def test_format_buffered_message_summary(buffer, metadata):
        buffer.buffer_message("ch1", "u1", "检查书架", MessageIntent.PLAYER_ACTION, metadata)
        buffer.buffer_message("ch1", "u1", "翻阅文件", MessageIntent.PLAYER_ACTION, metadata)
        summary = buffer.format_buffered_message_summary("ch1")
        assert summary is not None
        assert "Buffered Messages" in summary
    ```

    Test MessageBuffer directly - no async or model needed.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_message_buffer_flow.py -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>MessageBuffer correctly buffers during high-intensity phases and releases on transition</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Intent classification flow tests (ROUTER-03)</name>
  <files>tests/test_intent_classification_flow.py</files>
  <behavior>
    - test_should_buffer_intent_scene_round_resolving_player_action - PLAYER_ACTION buffered
    - test_should_buffer_intent_scene_round_resolving_admin_action - ADMIN_ACTION not buffered
    - test_should_buffer_intent_combat_player_action - PLAYER_ACTION not buffered in combat
    - test_should_buffer_intent_combat_ooc - OOC buffered in combat
    - test_get_intent_priority_scene_round_open - PLAYER_ACTION highest priority
    - test_get_intent_priority_lobby - ADMIN_ACTION highest priority
  </behavior>
  <action>
    Create tests/test_intent_classification_flow.py:

    ```python
    """ROUTER-03: Intent classification and buffering flow tests."""
    import pytest
    from dm_bot.router.intent import (
        MessageIntent, should_buffer_intent, get_intent_priority,
        get_handling_decision, MessageIntentMetadata,
    )

    def test_should_buffer_intent_scene_round_resolving_player_action():
        assert should_buffer_intent(MessageIntent.PLAYER_ACTION, "scene_round_resolving") is True

    def test_should_buffer_intent_scene_round_resolving_admin_action():
        assert should_buffer_intent(MessageIntent.ADMIN_ACTION, "scene_round_resolving") is False

    def test_should_buffer_intent_combat_player_action():
        # PLAYER_ACTION not buffered during combat (each action resolved immediately)
        assert should_buffer_intent(MessageIntent.PLAYER_ACTION, "combat") is False

    def test_should_buffer_intent_combat_ooc():
        assert should_buffer_intent(MessageIntent.OOC, "combat") is True

    def test_get_intent_priority_scene_round_open():
        priority = get_intent_priority(MessageIntent.PLAYER_ACTION, "scene_round_open")
        assert priority == 10  # highest

    def test_get_intent_priority_lobby():
        priority = get_intent_priority(MessageIntent.ADMIN_ACTION, "lobby")
        assert priority == 10  # highest in lobby

    def test_get_handling_decision_buffers_player_action():
        decision = get_handling_decision(MessageIntent.PLAYER_ACTION, "scene_round_resolving")
        assert "buffered" in decision.lower()

    def test_get_handling_decision_admin_not_buffered():
        decision = get_handling_decision(MessageIntent.ADMIN_ACTION, "scene_round_resolving")
        assert "immediately" in decision.lower() or "processed" in decision.lower()
    ```

    Test pure functions from router/intent.py - no async or model needed.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_intent_classification_flow.py -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>Intent classification correctly maps messages to intents and buffers appropriately per phase</done>
</task>

## Verification

After all tests written (RED phase):
```bash
uv run pytest tests/test_router_service_flow.py tests/test_message_buffer_flow.py tests/test_intent_classification_flow.py -v --tb=short
```

After implementation (GREEN phase):
```bash
uv run pytest tests/test_router_service_flow.py tests/test_message_buffer_flow.py tests/test_intent_classification_flow.py -v
```

## Success Criteria

1. **ROUTER-01 Covered:** RouterService.route() returns valid TurnPlan; invalid JSON raises RouterError
2. **ROUTER-02 Covered:** MessageBuffer correctly buffers/releases during phase transitions
3. **ROUTER-03 Covered:** should_buffer_intent() correctly gates by phase and intent
4. **All tests pass:** `uv run pytest tests/test_router_service_flow.py tests/test_message_buffer_flow.py tests/test_intent_classification_flow.py -v`

## Output

After completion, create `.planning/workstreams/track-e/phases/66-model-router/66-01-SUMMARY.md`
