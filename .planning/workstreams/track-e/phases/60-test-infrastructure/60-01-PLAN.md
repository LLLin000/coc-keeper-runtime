---
phase: 60-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/fakes/discord.py
  - tests/fakes/models.py
  - tests/test_ready_commands.py
  - tests/test_phase2_integration.py
  - tests/test_gameplay_integration.py
  - tests/test_investigator_panels.py
  - tests/test_dual_model_orchestration.py
  - tests/test_narration_service.py
  - tests/features/combat_round.feature
  - tests/bdd/test_combat_round_bdd.py
  - tests/cassettes/
  - src/dm_bot/persistence/store.py
  - pyproject.toml
autonomous: true
requirements:
  - E60-FAKE-INTERACTION
  - E60-MODEL-MOCKS
  - E60-VCR-BDD
  - E60-INMEMORY-SQLITE
---

<objective>
Establish the reusable test infrastructure base for Track E scenario testing.

Purpose: eliminate repeated Discord/mock boilerplate, make model behavior controllable across success/latency/failure paths, and create deterministic replay/scenario scaffolding before deeper phase-specific tests are added.

Output: a shared FakeInteraction factory, standard Fast/Slow/Error model fixtures, baseline VCR.py replay wiring, pytest-bdd scaffolding, and a default in-memory SQLite fixture contract for scenario tests.
</objective>

<execution_context>
@C:/Users/Lin/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/track-e/phases/60-test-infrastructure/60-CONTEXT.md
@.planning/workstreams/track-e/ROADMAP.md
@.planning/workstreams/track-e/codebase-architecture.md
@.planning/workstreams/track-e/test-coverage-survey.md
@.planning/workstreams/track-e/research-test-strategy.md
@tests/test_ready_commands.py
@tests/test_phase2_integration.py
@tests/test_gameplay_integration.py
@tests/test_investigator_panels.py
@tests/test_dual_model_orchestration.py
@tests/test_narration_service.py
@src/dm_bot/persistence/store.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared FakeInteraction and in-memory test fixtures</name>
  <files>tests/conftest.py, tests/fakes/discord.py, tests/test_ready_commands.py, tests/test_phase2_integration.py, tests/test_gameplay_integration.py, tests/test_investigator_panels.py, src/dm_bot/persistence/store.py</files>
  <read_first>
@tests/test_ready_commands.py
@tests/test_phase2_integration.py
@tests/test_gameplay_integration.py
@tests/test_investigator_panels.py
@src/dm_bot/persistence/store.py
  </read_first>
  <acceptance_criteria>
- `grep -R "def fake_interaction" tests/fakes/discord.py` finds the shared factory entry point.
- `grep -R "def fake_context" tests/fakes/discord.py` finds the context helper for command-style tests.
- `grep -R "@pytest.fixture" tests/conftest.py` shows the shared pytest fixture registration.
- `grep -R "sqlite_memory_store" tests/conftest.py` finds the default in-memory SQLite fixture name.
- `grep -R "from tests.fakes.discord import" tests/test_ready_commands.py tests/test_phase2_integration.py tests/test_gameplay_integration.py tests/test_investigator_panels.py` shows at least these four suites consume the shared factory instead of local FakeInteraction classes.
- `uv run pytest tests/test_ready_commands.py tests/test_phase2_integration.py tests/test_gameplay_integration.py tests/test_investigator_panels.py -q` exits 0.
  </acceptance_criteria>
  <action>
1. Create `tests/fakes/discord.py` with concrete helpers:
   - `fake_interaction(...)` returning a spec-safe mock object with `user`, `channel`, `guild`, `response.send_message`, `response.defer`, `followup.send`, and mutable `extras` payload support.
   - `fake_context(...)` returning a command-context style mock with `author`, `channel`, `guild`, `send`, and `reply` async mocks.
   - nested helper builders for fake user/channel/guild defaults so tests can override only needed fields.

2. Create or extend `tests/conftest.py` with reusable fixtures:
   - `interaction_factory`
   - `context_factory`
   - `sqlite_memory_store`
   - `sqlite_memory_path`

3. Update `src/dm_bot/persistence/store.py` only as needed so test infrastructure can instantiate `PersistenceStore(":memory:")` cleanly on Windows and normal file paths still work. If the constructor must widen from `Path` to `str | Path`, keep the production API backward compatible.

4. Replace duplicated local `FakeInteraction` or `_make_interaction` helpers in:
   - `tests/test_ready_commands.py`
   - `tests/test_phase2_integration.py`
   - `tests/test_gameplay_integration.py`
   - `tests/test_investigator_panels.py`
   with imports from `tests.fakes.discord` or the fixtures from `tests/conftest.py`.

5. Make the fixture contract explicit: scenario-oriented tests in this track default to in-memory SQLite isolation, not temp shared files.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add standard FastMock, SlowMock, and ErrorMock model fixtures</name>
  <files>tests/conftest.py, tests/fakes/models.py, tests/test_dual_model_orchestration.py, tests/test_narration_service.py</files>
  <read_first>
@tests/test_dual_model_orchestration.py
@tests/test_narration_service.py
@src/dm_bot/models/schemas.py
@src/dm_bot/narration/service.py
  </read_first>
  <acceptance_criteria>
- `grep -R "class FastMock" tests/fakes/models.py` finds the instant-response mock.
- `grep -R "class SlowMock" tests/fakes/models.py` finds the delayed-response mock.
- `grep -R "class ErrorMock" tests/fakes/models.py` finds the failure-path mock.
- `grep -R "def fast_model_mock" tests/conftest.py && grep -R "def slow_model_mock" tests/conftest.py && grep -R "def error_model_mock" tests/conftest.py` finds the shared fixtures.
- `grep -R "from tests.fakes.models import" tests/test_dual_model_orchestration.py tests/test_narration_service.py` shows existing model tests switched to the shared mocks.
- `uv run pytest tests/test_dual_model_orchestration.py tests/test_narration_service.py -q` exits 0.
  </acceptance_criteria>
  <action>
1. Create `tests/fakes/models.py` with a shared OpenAI/Ollama-style stub surface that exposes:
   - `FastMock(router_content=..., narrator_content=...)`
   - `SlowMock(router_content=..., narrator_content=..., delay_seconds=...)`
   - `ErrorMock(router_error=..., narrator_error=...)`
   - request capture lists for router/narrator calls so assertions can verify prompts and replay inputs.

2. Keep the method names aligned with the current service boundary:
   - `call_router()`
   - `call_narrator()`
   - `stream_narrator()` where needed

3. Register pytest fixtures in `tests/conftest.py`:
   - `fast_model_mock`
   - `slow_model_mock`
   - `error_model_mock`
   - each fixture should provide sane default Chinese COC-safe content so later tests can override only the specific payload.

4. Refactor `tests/test_dual_model_orchestration.py` and `tests/test_narration_service.py` to stop declaring one-off stub clients inline unless a test needs a file-local specialization beyond the shared mock modes.

5. Ensure `SlowMock` uses a real async delay path so later timeout/recovery tests can reuse it for race-window assertions.
  </action>
</task>

<task type="auto">
  <name>Task 3: Wire VCR.py replay and pytest-bdd scenario scaffolding</name>
  <files>pyproject.toml, tests/conftest.py, tests/cassettes/, tests/features/combat_round.feature, tests/bdd/test_combat_round_bdd.py, tests/test_narration_service.py</files>
  <read_first>
@tests/test_narration_service.py
@.planning/workstreams/track-e/research-test-strategy.md
@.planning/workstreams/track-e/test-coverage-survey.md
@pyproject.toml
  </read_first>
  <acceptance_criteria>
- `grep -n "pytest-bdd" pyproject.toml && grep -n "vcrpy" pyproject.toml` confirms both dependencies are declared.
- `grep -R "vcr" tests/conftest.py` finds the shared VCR configuration.
- `grep -R "Scenario:" tests/features/combat_round.feature` finds the starter Gherkin scenario.
- `grep -R "scenarios\|given\|when\|then" tests/bdd/test_combat_round_bdd.py` finds the pytest-bdd wiring.
- `grep -R "@pytest.mark.vcr\|use_cassette" tests/test_narration_service.py` finds the first cassette-backed narration replay test.
- `uv run pytest tests/test_narration_service.py tests/bdd/test_combat_round_bdd.py -q` exits 0.
  </acceptance_criteria>
  <action>
1. Add `vcrpy` and `pytest-bdd` to `pyproject.toml` test dependencies using the repository's existing dependency style.

2. Extend `tests/conftest.py` with shared VCR configuration:
   - cassette library dir: `tests/cassettes`
   - record mode suitable for committed deterministic fixtures (default to `once`)
   - filters for local-only host/authorization values if any model URL metadata appears in requests
   - a helper fixture that makes narration replay tests easy to author.

3. Add the first replay-oriented narration test in `tests/test_narration_service.py` that proves narration prompt assembly stays compatible with a recorded response path. The cassette should live under `tests/cassettes/` and the test should assert deterministic output handling rather than live model quality.

4. Create starter BDD scaffolding:
   - `tests/features/combat_round.feature`
   - `tests/bdd/test_combat_round_bdd.py`
   - scenario topic: a minimal combat/investigation round with Given/When/Then wording aligned to future Track E scenario plans.

5. Make the BDD scaffold use the shared FakeInteraction/model fixtures plus in-memory SQLite defaults, so later phase plans can extend scenarios without introducing a second setup path.
  </action>
</task>

</tasks>

<verification>
- [ ] Shared FakeInteraction/fake_context utilities exist under `tests/fakes/discord.py`
- [ ] Existing Discord-facing tests import shared fakes instead of redefining local interaction helpers
- [ ] In-memory SQLite fixture exists and is the documented scenario default
- [ ] Shared `FastMock`, `SlowMock`, and `ErrorMock` fixtures exist under `tests/fakes/models.py`
- [ ] `vcrpy` and `pytest-bdd` are configured in project test dependencies
- [ ] At least one narration replay test uses VCR cassettes
- [ ] At least one `.feature` file and one pytest-bdd step module exist under `tests/features/` and `tests/bdd/`
</verification>

<success_criteria>
- Later Track E plans can reuse one Discord factory instead of defining local fake interactions
- Later Track E plans can choose instant, delayed, or failing model behavior through shared fixtures
- Narration regression tests can replay deterministic captured responses without live local inference
- Scenario tests have an established Gherkin scaffold backed by shared fixtures and in-memory SQLite isolation
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-e/phases/60-test-infrastructure/60-01-SUMMARY.md`
</output>
