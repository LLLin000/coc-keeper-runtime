---
phase: "02"
plan: "01"
type: "standard"
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/characters/models.py
  - src/dm_bot/characters/sources.py
  - src/dm_bot/characters/importer.py
  - tests/test_character_import.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
  - CHAR-04
must_haves:
  truths:
    - "The runtime supports one clearly labeled snapshot import path from a mature external source."
    - "Imported character data is normalized into a local gameplay model rather than passed through raw."
    - "The import flow does not require a custom character-sheet editor in Discord."
  artifacts:
    - path: "src/dm_bot/characters/models.py"
      provides: "Normalized local gameplay character models."
    - path: "src/dm_bot/characters/sources.py"
      provides: "One external character-source adapter with snapshot semantics."
    - path: "src/dm_bot/characters/importer.py"
      provides: "Import orchestration and source labeling."
    - path: "tests/test_character_import.py"
      provides: "Automated verification for snapshot import and normalization."
---

<objective>
Implement the first v1 character path as a snapshot import adapter. The goal is low debugging cost, not broad provider coverage.
</objective>

<context>
Use a single source adapter. Prefer a structured provider pattern and keep test fixtures local. Normalize into a character model that already contains the fields Phase 3 combat will need.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized character models and snapshot import adapter</name>
  <files>src/dm_bot/characters/models.py, src/dm_bot/characters/sources.py, src/dm_bot/characters/importer.py, tests/test_character_import.py</files>
  <action>Introduce a normalized local character model with identity, abilities, HP, armor class, proficiency bonus, speed, skills, attacks, spellcasting summary, and resource counters. Add one snapshot adapter named and labeled explicitly as snapshot import, backed by local fixtures for deterministic tests. The importer should convert source data into the normalized model and retain source metadata.</action>
  <verify>
    <automated>uv run pytest tests/test_character_import.py -q</automated>
  </verify>
  <done>The runtime can import one external snapshot character and represent it locally for later rules execution.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_character_import.py -q`.
</verification>
