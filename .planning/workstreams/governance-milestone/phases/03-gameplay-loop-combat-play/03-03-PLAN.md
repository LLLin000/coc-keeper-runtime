---
phase: "03"
plan: "03"
type: "standard"
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/dm_bot/orchestrator/gameplay.py
  - src/dm_bot/orchestrator/turn_runner.py
  - src/dm_bot/discord_bot/commands.py
  - tests/test_gameplay_integration.py
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - RULE-02
  - RULE-03
must_haves:
  truths:
    - "Scene mode and combat mode are reachable from the existing Discord runtime."
    - "Turn execution validates gameplay state before narration."
    - "Discord replies remain readable during combat and multi-character scenes."
---

<objective>
Connect scene mode and combat play into the existing runtime so the bot can actually run a playable Discord session.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Integrate scene and combat play into the runtime turn path</name>
  <files>src/dm_bot/orchestrator/gameplay.py, src/dm_bot/orchestrator/turn_runner.py, src/dm_bot/discord_bot/commands.py, tests/test_gameplay_integration.py</files>
  <action>Wire the new gameplay-mode and combat modules into the existing turn pipeline and add a small command surface for switching scenes or starting combat. Ensure narration only sees verified gameplay outputs and speaker formatting.</action>
  <verify>
    <automated>uv run pytest tests/test_gameplay_integration.py -q</automated>
  </verify>
  <done>Phase 3 gameplay is reachable through the existing Discord runtime.</done>
</task>
</tasks>
