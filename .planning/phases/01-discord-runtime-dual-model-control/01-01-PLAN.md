---
phase: "01"
plan: "01"
type: "standard"
wave: 1
depends_on: []
requirements:
  - DISC-03
  - DISC-04
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - OPS-03
files_modified:
  - pyproject.toml
  - src/dm_bot/__init__.py
  - src/dm_bot/config.py
  - src/dm_bot/logging.py
  - src/dm_bot/models/schemas.py
  - src/dm_bot/models/ollama_client.py
  - src/dm_bot/runtime/health.py
  - src/dm_bot/runtime/app.py
  - tests/test_health.py
  - tests/test_model_schemas.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The codebase exposes a Python application skeleton for the Discord runtime and local model orchestration layer."
    - "Typed configuration, shared model transport, and common request/response envelopes exist and can be validated in code."
    - "A basic health or setup surface exists to verify service configuration and model connectivity before live play."
  artifacts:
    - path: "pyproject.toml"
      exists: true
    - path: "src/dm_bot/runtime/app.py"
      exists: true
    - path: "src/dm_bot/runtime/health.py"
      exists: true
    - path: "src/dm_bot/models/schemas.py"
      exists: true
    - path: "tests/test_health.py"
      exists: true
  key_links:
    - from: "src/dm_bot/runtime/app.py"
      to: "src/dm_bot/runtime/health.py"
      via: "app registers runtime health endpoints and checks"
    - from: "src/dm_bot/models/ollama_client.py"
      to: "src/dm_bot/models/schemas.py"
      via: "client validates request and response payloads with shared schemas"
---

# Phase 01 Plan 01: Runtime Foundation and Shared Model Transport

<objective>
Create the Python runtime skeleton, typed configuration, shared Ollama-facing model transport layer, and a basic health/setup surface so later plans can add Discord session behavior and higher-level router/narrator logic on a stable foundation.
</objective>

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

## Tasks

<task type="auto">
  <name>Bootstrap Python project structure and runtime entrypoints</name>
  <read_first>
    <file>.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md</file>
    <file>.planning/research/STACK.md</file>
    <file>.planning/research/ARCHITECTURE.md</file>
  </read_first>
  <action>Create a Python project skeleton in `pyproject.toml` and `src/dm_bot/` using Python 3.12+, `discord.py`, `fastapi`, `uvicorn`, `openai`, `pydantic`, `pydantic-settings`, `httpx`, `orjson`, `tenacity`, and `structlog`. Add package modules for runtime, models, and shared infrastructure. Implement `src/dm_bot/runtime/app.py` with an application factory that can build a FastAPI app and wire health routes without requiring Discord login during import time. Add `src/dm_bot/__init__.py`, `src/dm_bot/config.py`, and `src/dm_bot/logging.py` with a typed settings object and a central logger setup.</action>
  <files>pyproject.toml, src/dm_bot/__init__.py, src/dm_bot/config.py, src/dm_bot/logging.py, src/dm_bot/runtime/app.py</files>
  <verify>
    <automated>uv run pytest tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`pyproject.toml` contains dependencies for `discord.py`, `fastapi`, `uvicorn`, `openai`, `pydantic`, `pydantic-settings`, and `httpx`.</criterion>
    <criterion>`src/dm_bot/runtime/app.py` defines an application factory function for the runtime service.</criterion>
    <criterion>`src/dm_bot/config.py` defines a typed settings class that includes Discord token, Ollama base URL, router model name, and narrator model name fields.</criterion>
  </acceptance_criteria>
  <done>The repository contains a bootable Python runtime skeleton with typed settings and an import-safe FastAPI app factory.</done>
</task>

<task type="auto">
  <name>Implement typed model contracts and Ollama-compatible client layer</name>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md</file>
    <file>src/dm_bot/config.py</file>
  </read_first>
  <action>Add `src/dm_bot/models/schemas.py` with shared Pydantic models for health-check results and campaign-scoped turn envelopes, plus common request or response envelopes that later router and narrator services can compose. Add `src/dm_bot/models/ollama_client.py` that wraps OpenAI-compatible chat calls against Ollama and exposes separate configurable methods for router and narrator invocation using configured model names. Keep this plan responsible for base transport and shared typed envelopes only; higher-level router contracts and narration orchestration are owned by Plan 03.</action>
  <files>src/dm_bot/models/schemas.py, src/dm_bot/models/ollama_client.py, tests/test_model_schemas.py</files>
  <verify>
    <automated>uv run pytest tests/test_model_schemas.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/dm_bot/models/schemas.py` defines shared typed payload models used by later router and narrator services.</criterion>
    <criterion>`src/dm_bot/models/ollama_client.py` contains distinct methods or call paths for router and narrator model requests.</criterion>
    <criterion>The client layer reads model names from settings instead of hardcoding model identifiers in multiple files.</criterion>
  </acceptance_criteria>
  <done>The codebase has a reusable Ollama transport layer and shared typed payload models that later plans can build on.</done>
</task>

<task type="auto">
  <name>Expose runtime health and setup checks with tests</name>
  <read_first>
    <file>.planning/research/PITFALLS.md</file>
    <file>src/dm_bot/runtime/app.py</file>
    <file>src/dm_bot/models/ollama_client.py</file>
  </read_first>
  <action>Add `src/dm_bot/runtime/health.py` and wire it through `src/dm_bot/runtime/app.py` so operators can hit a basic health endpoint and a richer setup-check routine that validates configuration presence and model server reachability. Add tests in `tests/test_health.py` and `tests/test_model_schemas.py` to verify health route behavior and schema validation. Keep the setup-check non-destructive and suitable for later Discord command or operator use.</action>
  <files>src/dm_bot/runtime/health.py, src/dm_bot/runtime/app.py, tests/test_health.py, tests/test_model_schemas.py</files>
  <verify>
    <automated>uv run pytest tests/test_health.py tests/test_model_schemas.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/dm_bot/runtime/health.py` contains a health or setup-check function that reports configuration and model connectivity state.</criterion>
    <criterion>`src/dm_bot/runtime/app.py` exposes at least one route or handler that returns health data.</criterion>
    <criterion>`tests/test_health.py` and `tests/test_model_schemas.py` exist and pass.</criterion>
  </acceptance_criteria>
  <done>Operators have a non-destructive runtime health or setup surface with tests that validate configuration and model connectivity reporting.</done>
</task>

## Verification
- Run the project test command for the new test suite and confirm all Phase 1 foundation tests pass.
- Confirm the FastAPI app imports successfully and exposes the health surface without requiring a live Discord connection.
- Confirm router and narrator schemas validate example payloads separately.

## Success Criteria
- [ ] Python runtime skeleton exists with typed configuration and shared logging
- [ ] Shared model transport and common typed envelopes exist for later router and narrator services
- [ ] Health/setup surface exists for later operator checks
- [ ] Phase foundation tests pass
