# Plan 07-01: Encode 疯狂之馆 as the First Formal Module

## Goal

Ship `疯狂之馆` as a real structured adventure package and add only the runtime helpers needed to progress through its rooms, discoveries, counters, and endings.

## Tasks

### Task 1: Add failing tests for module loading and progression helpers
<files>
- `tests/test_adventure_loader.py`
- `tests/test_gameplay_integration.py`
</files>
<verify>
- `uv run pytest tests/test_adventure_loader.py tests/test_gameplay_integration.py -q`
</verify>
<done>
- Tests cover loading the formal module, scene movement, clue discovery, module-state mutation, and ending selection.
</done>

### Task 2: Add the 疯狂之馆 package data
<files>
- `src/dm_bot/adventures/mad_mansion.json`
- `src/dm_bot/adventures/__init__.py`
</files>
<verify>
- `uv run pytest tests/test_adventure_loader.py -q`
</verify>
<done>
- The formal package models the hall, branch rooms, major clues, key state variables, and endings.
</done>

### Task 3: Add thin runtime helpers for module progression
<files>
- `src/dm_bot/orchestrator/gameplay.py`
- `tests/test_gameplay_integration.py`
</files>
<verify>
- `uv run pytest tests/test_gameplay_integration.py -q`
</verify>
<done>
- Gameplay can move between scenes, record clue ids, update module counters, and choose endings for the formal module.
</done>

## Exit Criteria

- `疯狂之馆` loads as a formal package.
- Runtime can progress the module through canonical state updates instead of only static summaries.
- Endings and discoveries are represented as data, not freeform narration.
