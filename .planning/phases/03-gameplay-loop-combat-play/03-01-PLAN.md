---
phase: "03"
plan: "01"
type: "standard"
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/gameplay/modes.py
  - src/dm_bot/gameplay/scene_formatter.py
  - src/dm_bot/router/contracts.py
  - tests/test_scene_modes.py
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
must_haves:
  truths:
    - "The runtime distinguishes DM narration mode from scene-based multi-character performance."
    - "Scene outputs use explicit speaker attribution."
    - "The runtime can return from scene mode to DM-led play without losing mode context."
---

<objective>
Add gameplay-mode state and scene formatting so the runtime can produce readable DM narration and multi-character performance in Chinese-first output.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add mode state and scene speaker formatting</name>
  <files>src/dm_bot/gameplay/modes.py, src/dm_bot/gameplay/scene_formatter.py, src/dm_bot/router/contracts.py, tests/test_scene_modes.py</files>
  <action>Extend the gameplay layer with explicit mode state (`dm`, `scene`, `combat`), add optional speaker hints to the router contract, and implement a formatter that renders narrator output with stable speaker attribution for scene mode while preserving normal DM mode output. Keep the output Chinese-first and compact for Discord.</action>
  <verify>
    <automated>uv run pytest tests/test_scene_modes.py -q</automated>
  </verify>
  <done>The runtime can switch between DM mode and multi-character scene mode with explicit readable speaker output.</done>
</task>
</tasks>
