---
phase: "02"
plan: "02"
type: "standard"
wave: 2
depends_on:
  - "01"
files_modified:
  - src/dm_bot/rules/compendium.py
  - src/dm_bot/rules/engine.py
  - src/dm_bot/rules/actions.py
  - tests/test_rules_engine.py
autonomous: true
requirements:
  - RULE-01
  - RULE-04
  - RULE-05
  - RULE-06
must_haves:
  truths:
    - "The rules layer is deterministic and authoritative for state-changing mechanics."
    - "The compendium baseline is restricted to 2014 SRD data."
    - "Malformed model actions fail closed."
  artifacts:
    - path: "src/dm_bot/rules/compendium.py"
      provides: "2014 SRD lookup adapter with local test fixtures."
    - path: "src/dm_bot/rules/actions.py"
      provides: "Typed rule action contracts."
    - path: "src/dm_bot/rules/engine.py"
      provides: "Deterministic rules executor for lookup and mechanical validation."
    - path: "tests/test_rules_engine.py"
      provides: "Automated verification for lookup, baseline enforcement, and fail-closed execution."
---

<objective>
Build the deterministic rules backbone and a 2014 SRD compendium adapter that later gameplay work can call as the source of truth.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 2014 SRD compendium adapter and deterministic action executor</name>
  <files>src/dm_bot/rules/compendium.py, src/dm_bot/rules/actions.py, src/dm_bot/rules/engine.py, tests/test_rules_engine.py</files>
  <action>Implement typed action schemas for lookup and mechanical intents, add a compendium adapter backed by local 2014 SRD fixtures for tests, and build a deterministic rules engine that can execute or reject actions without relying on narrator output. Invalid actions or unsupported baselines must return explicit failures.</action>
  <verify>
    <automated>uv run pytest tests/test_rules_engine.py -q</automated>
  </verify>
  <done>The runtime has a deterministic rules backbone and a constrained compendium source for Phase 3 gameplay.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rules_engine.py -q`.
</verification>
