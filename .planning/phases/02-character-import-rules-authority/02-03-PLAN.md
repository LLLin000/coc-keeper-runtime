---
phase: "02"
plan: "03"
type: "standard"
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/dm_bot/orchestrator/gameplay.py
  - src/dm_bot/discord_bot/commands.py
  - src/dm_bot/orchestrator/turn_runner.py
  - tests/test_phase2_integration.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
  - RULE-01
  - RULE-04
  - RULE-05
  - RULE-06
must_haves:
  truths:
    - "Imported characters and deterministic rules are reachable through the existing Discord turn/runtime pipeline."
    - "Turn execution uses rules validation before narration."
    - "Character source labeling remains visible to operators."
  artifacts:
    - path: "src/dm_bot/orchestrator/gameplay.py"
      provides: "Phase 2 gameplay-facing orchestration helpers."
    - path: "tests/test_phase2_integration.py"
      provides: "End-to-end local verification from command/runtime path into rules and character layers."
---

<objective>
Attach the character and rules foundations to the existing runtime so Phase 3 can focus on gameplay behavior instead of missing infrastructure.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate imported characters and rules checks into the turn path</name>
  <files>src/dm_bot/orchestrator/gameplay.py, src/dm_bot/discord_bot/commands.py, src/dm_bot/orchestrator/turn_runner.py, tests/test_phase2_integration.py</files>
  <action>Add a thin gameplay orchestration layer that resolves imported characters, validates structured rule actions through the deterministic engine, and only then hands compact verified results to narration. Extend the command/runtime path so operators can import a snapshot character and inspect source labeling without new UI complexity.</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_integration.py -q</automated>
  </verify>
  <done>Phase 2 functionality is reachable from the existing runtime and ready for Phase 3 gameplay work.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_phase2_integration.py -q` and then `uv run pytest -q`.
</verification>
