# Plan 06-01: Formal Adventure Schema and Runtime State

## Goal

Replace the starter-only adventure schema with a validated formal package model, initialize canonical module state from package data, and expose structured public/gm adventure context to the rest of the runtime.

## Tasks

### Task 1: Add failing tests for formal adventure packages
<files>
- `tests/test_adventure_loader.py`
- `tests/test_gameplay_integration.py`
- `tests/test_narration_service.py`
</files>
<verify>
- `uv run pytest tests/test_adventure_loader.py tests/test_gameplay_integration.py tests/test_narration_service.py -q`
</verify>
<done>
- New tests cover package validation, canonical state initialization, and public-vs-gm narration context.
</done>

### Task 2: Implement the new adventure schema and loader validation
<files>
- `src/dm_bot/adventures/models.py`
- `src/dm_bot/adventures/loader.py`
- `src/dm_bot/adventures/starter_crypt.json`
</files>
<verify>
- `uv run pytest tests/test_adventure_loader.py -q`
</verify>
<done>
- Adventure packages model start scene, state fields, reveal policy, triggers/endings metadata, and validate correctly.
</done>

### Task 3: Initialize and export canonical module state in gameplay
<files>
- `src/dm_bot/orchestrator/gameplay.py`
- `src/dm_bot/orchestrator/turn_runner.py`
- `src/dm_bot/narration/service.py`
</files>
<verify>
- `uv run pytest tests/test_gameplay_integration.py tests/test_narration_service.py -q`
</verify>
<done>
- Gameplay can initialize module state, expose public/gm adventure snapshots, and keep narration input reveal-aware.
</done>

## Exit Criteria

- Formal adventure packages are validated before play.
- Gameplay can answer the active scene and current module state from canonical data.
- Narration receives both omniscient and reveal-safe module context without mutating module state.
