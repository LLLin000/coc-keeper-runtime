---
phase: 45-preflight-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/main.py
  - src/dm_bot/config.py
  - src/dm_bot/runtime/health.py
  - tests/test_health.py
  - tests/test_smoke_check.py
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "Operators can run preflight and see an explicit readiness contract for local runtime prerequisites."
    - "Preflight output is non-destructive and distinguishable from smoke-check behavior."
    - "Preflight reuses or aligns with runtime health semantics instead of inventing a conflicting readiness model."
  artifacts:
    - path: "src/dm_bot/main.py"
      provides: "Preflight CLI entrypoint"
    - path: "src/dm_bot/runtime/health.py"
      provides: "Shared readiness/health snapshot logic"
    - path: "tests/test_health.py"
      provides: "Verification for readiness output or health snapshot"
  key_links:
    - from: "src/dm_bot/main.py"
      to: "src/dm_bot/runtime/health.py"
      via: "preflight consumes shared readiness logic"
    - from: "tests/test_health.py"
      to: "src/dm_bot/main.py"
      via: "tests verify preflight readiness contract"
---

<objective>
Turn preflight into a reliable operator readiness contract that reports local prerequisites clearly before smoke-check or bot startup is attempted.

Purpose: Establish the first stable ops-facing readiness surface for `track-ops / v1.0`.
Output: clearer preflight behavior, aligned readiness logic, and regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/track-ops/ROADMAP.md
@.planning/workstreams/track-ops/STATE.md
@.planning/workstreams/track-ops/REQUIREMENTS.md
@.planning/workstreams/track-ops/phases/45-preflight-contract/45-CONTEXT.md
@.planning/workstreams/track-ops/phases/45-preflight-contract/45-RESEARCH.md
@src/dm_bot/main.py
@src/dm_bot/config.py
@src/dm_bot/runtime/health.py
@src/dm_bot/runtime/control_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and expose a preflight readiness contract</name>
  <files>src/dm_bot/main.py, src/dm_bot/runtime/health.py, src/dm_bot/config.py</files>
  <read_first>src/dm_bot/main.py, src/dm_bot/runtime/health.py, src/dm_bot/config.py, .planning/workstreams/track-ops/phases/45-preflight-contract/45-CONTEXT.md</read_first>
  <action>Refactor preflight so it reports readiness categories explicitly: Discord configuration, local model endpoint/model names, and asset-root visibility. Reuse or align with shared health-snapshot logic instead of leaving preflight as an isolated string dump. Keep the command non-destructive and terminal-readable.</action>
  <verify>uv run pytest tests/test_health.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/main.py` contains a preflight path that reports explicit readiness categories
    - `src/dm_bot/runtime/health.py` participates in the readiness logic or provides aligned snapshot semantics
  </acceptance_criteria>
  <done>Preflight exposes a stable readiness contract instead of an ad hoc configuration dump.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the boundary between preflight and smoke-check</name>
  <files>src/dm_bot/main.py, tests/test_smoke_check.py, tests/test_health.py</files>
  <read_first>src/dm_bot/main.py, src/dm_bot/runtime/smoke_check.py, tests/test_smoke_check.py, tests/test_health.py</read_first>
  <action>Update tests and CLI behavior so preflight remains a non-destructive readiness check and does not silently drift into smoke-check responsibilities. Add or revise tests to verify the distinction between readiness reporting and boot/test execution behavior.</action>
  <verify>uv run pytest tests/test_health.py tests/test_smoke_check.py -q</verify>
  <acceptance_criteria>
    - tests explicitly distinguish preflight from smoke-check behavior
    - preflight changes do not require pytest execution or bot boot to produce output
  </acceptance_criteria>
  <done>The repository has a tested separation between preflight and smoke-check responsibilities.</done>
</task>

<task type="auto">
  <name>Task 3: Make operator output actionable</name>
  <files>src/dm_bot/main.py, src/dm_bot/runtime/health.py, tests/test_health.py</files>
  <read_first>src/dm_bot/main.py, src/dm_bot/runtime/health.py, src/dm_bot/runtime/control_service.py</read_first>
  <action>Ensure preflight output uses concrete operator language so missing Discord config, missing models, or missing asset roots are clearly actionable. Reuse the same naming conventions that later control surfaces can consume or mirror. Keep the output stable enough that later ops phases can build on it.</action>
  <verify>uv run pytest tests/test_health.py -q</verify>
  <acceptance_criteria>
    - preflight output clearly reports missing versus configured state for the key operator prerequisites
    - tests cover at least one missing-prerequisite path and one healthy path
  </acceptance_criteria>
  <done>Operators can tell what is ready and what is missing before they attempt startup or smoke-check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_health.py tests/test_smoke_check.py -q`
- [ ] preflight remains non-destructive
- [ ] readiness output is explicit enough for later operator tooling to reuse
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preflight is a stable operator readiness surface, distinct from smoke-check
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-ops/phases/45-preflight-contract/45-01-SUMMARY.md`
</output>
