---
phase: 30-session-board-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/orchestrator/player_status_renderer.py
  - src/dm_bot/orchestrator/kp_ops_renderer.py
  - src/dm_bot/orchestrator/visibility.py
  - src/dm_bot/discord_bot/commands.py
  - tests/orchestrator/test_visibility.py
  - tests/orchestrator/test_kp_ops_renderer.py
autonomous: true
requirements:
  - SUR-01
user_setup: []
must_haves:
  truths:
    - "Players can inspect current session identity, phase, and waiting state through a dedicated board-like surface."
    - "Operators can inspect the same session through a surface that still respects audience differences."
    - "The surface consumes canonical runtime visibility/state instead of inventing parallel status truth."
  artifacts:
    - path: "src/dm_bot/orchestrator/player_status_renderer.py"
      provides: "Player-facing session board rendering"
    - path: "src/dm_bot/orchestrator/kp_ops_renderer.py"
      provides: "Operator-facing session board rendering"
    - path: "tests/orchestrator/test_visibility.py"
      provides: "Visibility/session board verification"
  key_links:
    - from: "src/dm_bot/orchestrator/visibility.py"
      to: "src/dm_bot/orchestrator/player_status_renderer.py"
      via: "renderer consumes visibility snapshots"
    - from: "src/dm_bot/orchestrator/visibility.py"
      to: "src/dm_bot/orchestrator/kp_ops_renderer.py"
      via: "operator renderer consumes visibility snapshots"
---

<objective>
Create the first reusable session-board surface for players and operators by rendering current session identity, phase, and blocker state from canonical runtime visibility snapshots.

Purpose: Establish the first real board primitive for `track-surface / v1.0`.
Output: board-aware session renderers, command integration, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/track-surface/ROADMAP.md
@.planning/workstreams/track-surface/STATE.md
@.planning/workstreams/track-surface/REQUIREMENTS.md
@.planning/workstreams/track-surface/phases/30-session-board-core/30-CONTEXT.md
@.planning/workstreams/track-surface/phases/30-session-board-core/30-RESEARCH.md
@src/dm_bot/orchestrator/visibility.py
@src/dm_bot/orchestrator/player_status_renderer.py
@src/dm_bot/orchestrator/kp_ops_renderer.py
@src/dm_bot/discord_bot/commands.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the session-board rendering contract</name>
  <files>src/dm_bot/orchestrator/player_status_renderer.py, src/dm_bot/orchestrator/kp_ops_renderer.py, src/dm_bot/orchestrator/visibility.py</files>
  <read_first>src/dm_bot/orchestrator/visibility.py, src/dm_bot/orchestrator/player_status_renderer.py, src/dm_bot/orchestrator/kp_ops_renderer.py, .planning/workstreams/track-surface/phases/30-session-board-core/30-CONTEXT.md</read_first>
  <action>Refactor or extend the existing player and KP renderers so they expose a clearer session-board contract centered on current campaign/session identity, phase, pending participants, and blocker summaries. Keep the source of truth in the visibility snapshot model, and avoid adding presentation-only status inference that runtime does not own.</action>
  <verify>uv run pytest tests/orchestrator/test_visibility.py tests/orchestrator/test_kp_ops_renderer.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/orchestrator/player_status_renderer.py` contains explicit board-oriented rendering for current session state
    - `src/dm_bot/orchestrator/kp_ops_renderer.py` preserves operator-specific visibility while using the same underlying session truth
  </acceptance_criteria>
  <done>Both player and operator surfaces can render a consistent session board from shared runtime state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the board surface into a usable command path</name>
  <files>src/dm_bot/discord_bot/commands.py, src/dm_bot/orchestrator/player_status_renderer.py, src/dm_bot/orchestrator/kp_ops_renderer.py</files>
  <read_first>src/dm_bot/discord_bot/commands.py, src/dm_bot/orchestrator/player_status_renderer.py, src/dm_bot/orchestrator/kp_ops_renderer.py</read_first>
  <action>Update the relevant command path so the new session-board presentation can actually be requested from Discord without bypassing existing visibility or role-routing rules. Reuse existing command surfaces where possible instead of introducing a brand-new command unless the current command structure cannot express the board cleanly.</action>
  <verify>uv run pytest tests/test_discord_commands.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/discord_bot/commands.py` contains a command path that uses the board-aware renderers
    - command integration continues to respect player/operator audience boundaries
  </acceptance_criteria>
  <done>The session board is reachable through an existing or clearly justified command entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for current-session board behavior</name>
  <files>tests/orchestrator/test_visibility.py, tests/orchestrator/test_kp_ops_renderer.py, tests/test_discord_commands.py</files>
  <read_first>tests/orchestrator/test_visibility.py, tests/orchestrator/test_kp_ops_renderer.py, tests/test_discord_commands.py, src/dm_bot/orchestrator/visibility.py</read_first>
  <action>Add tests that verify current session identity, waiting state, and blocker summaries render through the new board surface for both player and operator audiences. Keep the tests scoped to current-session board behavior; clue/history and consequence publication belong to later phases.</action>
  <verify>uv run pytest tests/orchestrator/test_visibility.py tests/orchestrator/test_kp_ops_renderer.py tests/test_discord_commands.py -q</verify>
  <acceptance_criteria>
    - `tests/orchestrator/test_visibility.py` covers session board data expectations
    - `tests/orchestrator/test_kp_ops_renderer.py` covers operator-facing board output
  </acceptance_criteria>
  <done>The new session-board surface is protected by targeted regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/orchestrator/test_visibility.py tests/orchestrator/test_kp_ops_renderer.py tests/test_discord_commands.py -q`
- [ ] session board output is derived from visibility/runtime truth, not ad hoc strings
- [ ] clue/history/consequence surfaces are not prematurely folded into this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A reusable current-session board exists for both player and operator audiences
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-surface/phases/30-session-board-core/30-01-SUMMARY.md`
</output>
