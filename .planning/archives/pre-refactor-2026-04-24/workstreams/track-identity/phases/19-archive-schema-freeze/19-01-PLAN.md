---
phase: 19-archive-schema-freeze
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/coc/archive.py
  - src/dm_bot/characters/models.py
  - tests/test_character_archive_flow.py
  - .planning/workstreams/track-identity/v1.0-SPEC.md
autonomous: true
requirements:
  - IDN-01
user_setup: []
must_haves:
  truths:
    - "Archive schema versioning is explicit and documented."
    - "Archive and character profile models do not silently disagree on canonical identity fields."
    - "Schema-level verification exists in tests rather than relying only on behavioral coverage."
  artifacts:
    - path: "src/dm_bot/coc/archive.py"
      provides: "Canonical archive schema and version defaults"
    - path: "src/dm_bot/characters/models.py"
      provides: "Identity-side investigator profile schema"
    - path: ".planning/workstreams/track-identity/v1.0-SPEC.md"
      provides: "Documented archive schema contract"
  key_links:
    - from: "src/dm_bot/coc/archive.py"
      to: "src/dm_bot/characters/models.py"
      via: "archive profile embeds the investigator schema"
    - from: ".planning/workstreams/track-identity/v1.0-SPEC.md"
      to: "src/dm_bot/coc/archive.py"
      via: "spec documents the frozen field contract"
---

<objective>
Freeze the archive schema and document its field contract so later identity work builds on a stable, explicit truth boundary.

Purpose: Establish the canonical schema baseline for `track-identity / v1.0`.
Output: explicit schema/version contract, aligned archive-character models, and schema-aware tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/track-identity/ROADMAP.md
@.planning/workstreams/track-identity/STATE.md
@.planning/workstreams/track-identity/REQUIREMENTS.md
@.planning/workstreams/track-identity/identity-DECISIONS.md
@.planning/workstreams/track-identity/phases/19-archive-schema-freeze/19-CONTEXT.md
@.planning/workstreams/track-identity/phases/19-archive-schema-freeze/19-RESEARCH.md
@src/dm_bot/coc/archive.py
@src/dm_bot/characters/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock archive schema version and align model boundaries</name>
  <files>src/dm_bot/coc/archive.py, src/dm_bot/characters/models.py</files>
  <read_first>src/dm_bot/coc/archive.py, src/dm_bot/characters/models.py, .planning/workstreams/track-identity/identity-DECISIONS.md</read_first>
  <action>Review the current archive model and investigator profile model, then make schema versioning explicit and consistent. Update `archive.py` and `characters/models.py` so canonical identity fields and schema-version semantics are intentional rather than incidental. Do not introduce session-checkpoint or migration execution logic in this phase; keep the change focused on freezing the schema contract.</action>
  <verify>uv run pytest tests/test_character_archive_flow.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/coc/archive.py` contains an explicit schema-version policy for archive profiles
    - `src/dm_bot/characters/models.py` contains any schema fields needed to keep archive and investigator models aligned
  </acceptance_criteria>
  <done>Archive and investigator profile models express a stable, explicit schema contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the frozen archive schema contract</name>
  <files>.planning/workstreams/track-identity/v1.0-SPEC.md</files>
  <read_first>.planning/workstreams/track-identity/ROADMAP.md, src/dm_bot/coc/archive.py, src/dm_bot/characters/models.py</read_first>
  <action>Create or update `v1.0-SPEC.md` under `track-identity` to document archive field ownership, schema-version expectations, and the boundary between archive truth and campaign-instance truth. Make the document concrete enough that later phases can use it as a source of truth instead of reverse-engineering the models again.</action>
  <verify>rg -n \"schema_version|archive|campaign-instance|field\" .planning/workstreams/track-identity/v1.0-SPEC.md</verify>
  <acceptance_criteria>
    - `.planning/workstreams/track-identity/v1.0-SPEC.md` exists
    - the spec explicitly documents schema versioning and archive-vs-campaign boundary
  </acceptance_criteria>
  <done>The archive schema freeze is documented in a reusable spec file.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema-aware verification</name>
  <files>tests/test_character_archive_flow.py</files>
  <read_first>tests/test_character_archive_flow.py, src/dm_bot/coc/archive.py, src/dm_bot/characters/models.py</read_first>
  <action>Add or update tests so schema defaults, version consistency, and archive-model alignment are checked explicitly. Keep the tests narrow and structural; builder-flow behavior belongs to later phases.</action>
  <verify>uv run pytest tests/test_character_archive_flow.py -q</verify>
  <acceptance_criteria>
    - `tests/test_character_archive_flow.py` contains assertions covering schema/version behavior
    - archive tests pass after the schema freeze updates
  </acceptance_criteria>
  <done>The frozen schema contract is protected by explicit automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_character_archive_flow.py -q`
- [ ] archive schema versioning is explicit in code and documentation
- [ ] session-checkpoint or migration logic is not prematurely implemented in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The identity layer has a documented and testable schema baseline
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-identity/phases/19-archive-schema-freeze/19-01-SUMMARY.md`
</output>
