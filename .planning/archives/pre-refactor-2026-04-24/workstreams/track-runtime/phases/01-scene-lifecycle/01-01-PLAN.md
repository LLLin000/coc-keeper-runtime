---
phase: 01-scene-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dm_bot/orchestrator/session_store.py
  - src/dm_bot/orchestrator/gameplay.py
  - src/dm_bot/gameplay/modes.py
  - tests/test_multi_user_session.py
  - tests/test_ready_gate.py
autonomous: true
requirements:
  - RTR-01
  - RTR-02
user_setup: []
must_haves:
  truths:
    - "The runtime has an explicit scene lifecycle model separate from session phase."
    - "Player focus and scene lifecycle are represented as different runtime concepts."
    - "Existing single-scene session behavior still works after the new model is introduced."
  artifacts:
    - path: "src/dm_bot/orchestrator/session_store.py"
      provides: "Canonical lifecycle-aware session/runtime state"
    - path: "src/dm_bot/orchestrator/gameplay.py"
      provides: "Gameplay integration with scene lifecycle state"
    - path: "tests/test_multi_user_session.py"
      provides: "Regression coverage for session behavior"
  key_links:
    - from: "src/dm_bot/orchestrator/session_store.py"
      to: "src/dm_bot/orchestrator/gameplay.py"
      via: "scene lifecycle state is consumed by gameplay runtime"
    - from: "src/dm_bot/orchestrator/session_store.py"
      to: "tests/test_multi_user_session.py"
      via: "tests validate lifecycle-aware state transitions"
---

<objective>
Introduce explicit scene lifecycle and focus models into the runtime so later shared-scene batching work can build on structured state instead of ad hoc dict growth and message-timing assumptions.

Purpose: Establish the canonical state boundary for `track-runtime / v1.0`.
Output: lifecycle-aware runtime state, gameplay integration, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/track-runtime/ROADMAP.md
@.planning/workstreams/track-runtime/STATE.md
@.planning/workstreams/track-runtime/REQUIREMENTS.md
@.planning/workstreams/track-runtime/phases/01-scene-lifecycle/01-CONTEXT.md
@.planning/workstreams/track-runtime/phases/01-scene-lifecycle/01-RESEARCH.md
@src/dm_bot/orchestrator/session_store.py
@src/dm_bot/orchestrator/gameplay.py
@src/dm_bot/gameplay/modes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit scene lifecycle and focus models</name>
  <files>src/dm_bot/orchestrator/session_store.py, src/dm_bot/gameplay/modes.py</files>
  <read_first>src/dm_bot/orchestrator/session_store.py, src/dm_bot/gameplay/modes.py, .planning/workstreams/track-runtime/phases/01-scene-lifecycle/01-CONTEXT.md</read_first>
  <action>Introduce explicit runtime models for scene lifecycle and player focus. Add concrete lifecycle states for scene collection and resolution work, and keep them separate from the existing session phase enum. Update `session_store.py` to carry this structured state without removing current single-scene behavior. If a new helper model is needed, place it in an existing runtime-facing module rather than scattering more raw dict keys across the store.</action>
  <verify>uv run pytest tests/test_multi_user_session.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/orchestrator/session_store.py` contains a dedicated scene lifecycle concept separate from session phase
    - `src/dm_bot/gameplay/modes.py` contains lifecycle/focus terminology that matches the new runtime model
  </acceptance_criteria>
  <done>The runtime carries explicit scene lifecycle and focus state instead of relying only on session phase and mutable dict conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate lifecycle state into gameplay runtime without breaking current flows</name>
  <files>src/dm_bot/orchestrator/gameplay.py, src/dm_bot/orchestrator/session_store.py</files>
  <read_first>src/dm_bot/orchestrator/gameplay.py, src/dm_bot/orchestrator/session_store.py, src/dm_bot/orchestrator/turns.py</read_first>
  <action>Update gameplay integration so the new scene lifecycle state is visible to the runtime where session/adventure state is read or published. Preserve existing single-scene defaults, but remove assumptions that scene lifecycle can only be inferred from current session phase or one-off flags. Do not implement merge behavior or cross-cut narration in this task; only integrate the state model so later phases can extend it cleanly.</action>
  <verify>uv run pytest tests/test_ready_gate.py tests/test_multi_user_session.py -q</verify>
  <acceptance_criteria>
    - `src/dm_bot/orchestrator/gameplay.py` reads or publishes the new lifecycle-aware state
    - gameplay integration does not introduce new raw `scene lifecycle` meaning through unrelated ad hoc fields
  </acceptance_criteria>
  <done>Gameplay code can consume the new scene lifecycle model without regressing current session behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the behavior with regression tests</name>
  <files>tests/test_multi_user_session.py, tests/test_ready_gate.py</files>
  <read_first>tests/test_multi_user_session.py, tests/test_ready_gate.py, src/dm_bot/orchestrator/session_store.py</read_first>
  <action>Add or update tests to verify that the runtime now exposes explicit scene lifecycle state while preserving existing single-scene behavior. Cover at least one lifecycle transition and one invariant showing that player focus and scene lifecycle are distinct concepts.</action>
  <verify>uv run pytest tests/test_multi_user_session.py tests/test_ready_gate.py -q</verify>
  <acceptance_criteria>
    - `tests/test_multi_user_session.py` contains assertions about lifecycle-aware state
    - `tests/test_ready_gate.py` still passes with the new lifecycle model in place
  </acceptance_criteria>
  <done>Regression tests prove that the lifecycle model exists and existing behavior still holds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_multi_user_session.py tests/test_ready_gate.py -q`
- [ ] lifecycle and focus are modeled as separate concepts in runtime code
- [ ] no merge or reveal behavior is prematurely implemented in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Scene lifecycle is a canonical runtime model instead of an implicit convention
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-runtime/phases/01-scene-lifecycle/01-01-SUMMARY.md`
</output>
