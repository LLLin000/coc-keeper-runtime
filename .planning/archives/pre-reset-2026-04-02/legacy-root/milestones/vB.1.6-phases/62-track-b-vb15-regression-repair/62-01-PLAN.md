---
phase: 62-track-b-vb15-regression-repair
plan: 01
type: repair
wave: 1
depends_on:
  - 61
files_modified:
  - src/dm_bot/coc/archive.py
  - src/dm_bot/orchestrator/session_store.py
  - src/dm_bot/discord_bot/commands.py
  - tests/test_archive_delete_recover.py
  - tests/test_instance_management.py
  - tests/test_ready_gate.py
  - tests/test_e2e_lifecycle.py
  - tests/test_ai_contract.py
  - tests/test_scenarios.py
autonomous: false
requirements:
  - PLC-05
  - PLC-06
  - ILC-02
  - ILC-03
  - PV-02
  - PV-04
  - AV-06
  - AV-07
  - AUD-01
  - AUD-02
  - AUD-03
must_haves:
  truths:
    - "Archive profiles support deleted_at-backed soft delete, grace-period recover, and purge"
    - "CampaignCharacterInstance has explicit status and SessionStore exposes lifecycle/governance APIs expected by Track B tests"
    - "Ready gate and command surfaces use instance truth consistently"
    - "Track B lifecycle tests, scenario tests, and repository gate pass together"
---

<objective>
Repair the merged Track B regressions at the contract boundary instead of masking individual tests.
</objective>

<context>
@.planning/milestones/vB.1.6-phases/62-track-b-vb15-regression-repair/62-CONTEXT.md
@src/dm_bot/coc/archive.py
@src/dm_bot/orchestrator/session_store.py
@src/dm_bot/orchestrator/governance_event_log.py
@src/dm_bot/discord_bot/commands.py
@tests/test_archive_delete_recover.py
@tests/test_instance_management.py
@tests/test_ready_gate.py
@tests/test_e2e_lifecycle.py
@tests/test_ai_contract.py
@tests/test_scenarios.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore archive lifecycle contracts</name>
  <files>src/dm_bot/coc/archive.py, tests/test_archive_delete_recover.py</files>
  <behavior>
    - `deleted_at` exists and defaults to `None`
    - deleted profiles render deleted state in summary/detail output
    - delete/recover/purge behavior matches the grace-period contract
    - active-profile replacement archives the previous profile instead of inventing a new status
  </behavior>
  <verify>
    <automated>uv run pytest -q tests/test_archive_delete_recover.py</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore session instance lifecycle and governance APIs</name>
  <files>src/dm_bot/orchestrator/session_store.py, tests/test_instance_management.py, tests/test_ready_gate.py</files>
  <behavior>
    - `CampaignCharacterInstance.status` exists and drives active-instance filtering
    - `retire_instance`, `select_instance_profile`, `force_archive_instance`, and `reassign_ownership` satisfy test contracts
    - `SessionStore` wires `GovernanceEventLog` and logs lifecycle/admin operations
    - `validate_ready()` checks active instance truth instead of stale membership fields
  </behavior>
  <verify>
    <automated>uv run pytest -q tests/test_instance_management.py tests/test_ready_gate.py</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile command and presentation surfaces</name>
  <files>src/dm_bot/discord_bot/commands.py, tests/test_v18_archive_builder.py, tests/test_e2e_lifecycle.py</files>
  <behavior>
    - `/profile_detail` and profile list/admin surfaces only depend on restored archive/session contracts
    - presentation shape matches the existing test contract where it is intentionally user-visible
    - command-level lifecycle/admin flows align with restored governance APIs
  </behavior>
  <verify>
    <automated>uv run pytest -q tests/test_v18_archive_builder.py tests/test_e2e_lifecycle.py tests/test_discord_commands.py tests/test_commands.py</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Reconcile scenario and contract flow</name>
  <files>src/dm_bot/orchestrator/session_store.py, src/dm_bot/discord_bot/commands.py, tests/test_ai_contract.py, tests/test_scenarios.py</files>
  <behavior>
    - scenario transitions reach ready/onboarding/scene phases under the repaired instance model
    - audience split and scenario contract tests assert the restored public behavior rather than a broken intermediate state
  </behavior>
  <verify>
    <automated>uv run pytest -q tests/test_ai_contract.py tests/test_scenarios.py</automated>
  </verify>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Run repository gate</name>
  <verify>
    <automated>uv run pytest -q</automated>
    <automated>uv run python -m dm_bot.main smoke-check</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Archive lifecycle tests pass without weakening the delete/recover contract
- Session instance/governance tests pass with event logging restored
- Profile detail and ready gate align with the instance-based lifecycle model
- Scenario/contract tests and the full repository gate pass
</success_criteria>

<output>
After completion, create `.planning/milestones/vB.1.6-phases/62-track-b-vb15-regression-repair/62-01-SUMMARY.md`
</output>
