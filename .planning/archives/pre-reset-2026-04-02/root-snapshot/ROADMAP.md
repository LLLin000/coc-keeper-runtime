# Pre-Reset Root Snapshot — `ROADMAP.md`

Immediately before reset, the root roadmap had already been reduced to a repository planning index rather than a single execution roadmap.

Its key guidance was:

1. read `PROJECT.md`
2. read `active-workstream`
3. read `workstreams/<track>/ROADMAP.md`
4. read `workstreams/<track>/STATE.md`

It also recorded the 5-track model and recommended work ordering:

1. stabilize shared runtime semantics
2. then improve presentation
3. then decide whether Track E should stay broad or split

This snapshot is preserved here because that guidance explains why the reset moved toward a 4-track baseline rather than continuing the old workstream structure.
