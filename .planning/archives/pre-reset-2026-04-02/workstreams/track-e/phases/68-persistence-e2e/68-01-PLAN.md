---
phase: 68-persistence-e2e
plan: "01"
type: e2e
wave: 1
depends_on: ["67-narration-pipeline/67-01", "65-character-archive/65-01", "64-rules-engine/64-01", "63-adventure-runtime/63-01"]
files_modified:
  - tests/test_e2e_15turn_scenario.py
  - tests/test_chaos_lobby_stress.py
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02
  - SCEN-01
  - SCEN-02
  - SCEN-03
  - SCEN-04
must_haves:
  truths:
    - "15-turn fuzhe scenario runs end-to-end with all layers wired (no live Discord)"
    - "DB recovery: reload from PersistenceStore produces identical session state"
    - "Chaos lobby: 5 concurrent users bind/join/ready without race conditions"
    - "Smoke-check failure recovery: bot restart re-initializes all layers correctly"
  artifacts:
    - path: tests/test_e2e_15turn_scenario.py
      provides: SCEN-01/02/03 full 15-turn e2e scenario tests
      min_lines: 120
    - path: tests/test_chaos_lobby_stress.py
      provides: SCEN-04 chaos lobby stress test
      min_lines: 80
  key_links:
    - from: test_e2e_15turn_scenario.py
      to: persistence/store.py
      via: PersistenceStore save/load round-trip
    - from: test_e2e_15turn_scenario.py
      to: orchestrator/session_store.py
      via: Full campaign lifecycle (bind→join→ready→play→end)
    - from: test_chaos_lobby_stress.py
      to: orchestrator/session_store.py
      via: Concurrent 5-user bind/join/ready
---

<objective>
Run the complete 15-turn fuzhe scenario end-to-end with all layers wired, plus a Chaos lobby stress test with 5 concurrent users.

Purpose: Final integration validation for vE.2.1. All layers must work together without live Discord connection.

Output: 2 e2e test files covering full scenario (SCEN-01/02/03) and chaos stress (SCEN-04).
</objective>

<execution_context>
@C:/Users/Lin/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/dm_bot/persistence/store.py
@src/dm_bot/orchestrator/session_store.py
@src/dm_bot/adventures/fuzhe.py
@tests/test_persistence_store.py
@tests/test_ready_commands.py
</context>

<feature>
  <name>15-Turn End-to-End Scenario (SCEN-01/02/03)</name>
  <files>tests/test_e2e_15turn_scenario.py</files>
  <behavior>
    - Test: 15 turns of fuzhe scenario execute without error
    - Test: After turn 15, session state matches expected end state
    - Test: DB recovery: save mid-scenario, reload, continue → identical results
    - Test: Smoke-check failure recovery: re-initialize all layers after simulated crash
    - Test: Streaming interruption recovery: "重新生成" resumes correctly
  </behavior>
  <implementation>
    Use in-memory SQLite + FastMock model. Simulate 15 turns with deterministic input.
    Test save/load round-trip mid-scenario.
  </implementation>
</feature>

<feature>
  <name>Chaos Lobby Stress Test (SCEN-04)</name>
  <files>tests/test_chaos_lobby_stress.py</files>
  <behavior>
    - Test: 5 concurrent users bind/join/select_profile/ready simultaneously
    - Test: All 5 users reach SCENE_ROUND_OPEN without race conditions
    - Test: No duplicate member_ids after concurrent join
    - Test: Phase transitions correct under concurrent load
  </behavior>
  <implementation>
    Use concurrent.futures.ThreadPoolExecutor to simulate 5 users hitting SessionStore simultaneously.
    Verify no duplicate members, all phases correct.
  </implementation>
</feature>

## Task Dependency Graph

| Task | Depends On | Reason |
|------|------------|--------|
| Task 1: 15-turn e2e scenario tests (SCEN-01/02/03) | E63, E64, E65, E67 | Needs adventure + rules + characters + narration |
| Task 2: Chaos lobby stress tests (SCEN-04) | E62 | Needs multi-user session |

## Parallel Execution Graph

Wave 1 (After E63/64/65/67 complete):
├── Task 1: 15-turn e2e scenario tests
└── Task 2: Chaos lobby stress tests

**Note:** These tasks depend on E63, E64, E65, E67 completion. Run after those phases.

## Commit Strategy

### Commit 1: RED - 15-turn e2e scenario tests (SCEN-01/02/03)
```
test(e2e-68): add failing 15-turn fuzhe e2e scenario tests (SCEN-01/02/03)

- test_15turn_fuzhe_scenario_completes
- test_db_recovery_mid_scenario
- test_smoke_check_failure_recovery
```

### Commit 2: RED - Chaos lobby stress test (SCEN-04)
```
test(e2e-68): add failing chaos lobby stress test (SCEN-04)

- test_five_concurrent_users_bind_join_ready
- test_no_duplicate_members_under_concurrent_load
```

### Commit 3: GREEN - Verify all tests pass
```
feat(e2e-68): e2e integration tests passing - PERSIST-01/02, SCEN-01/02/03/04 complete
```

## Tasks

<task type="auto" tdd="false">
  <name>Task 1: 15-turn e2e fuzhe scenario tests (SCEN-01/02/03)</name>
  <files>tests/test_e2e_15turn_scenario.py</files>
  <behavior>
    - test_15turn_fuzhe_scenario_completes - 15 turns of fuzhe execute without exception
    - test_end_state_matches_expectation - session_phase, active_characters, location at turn 15
    - test_db_recovery_mid_scenario - save at turn 8, reload, continue → same end state
    - test_recovery_after_simulated_crash - re-init all layers, reload session, continue
    - test_streaming_interruption_recovery - simulate interruption at turn 10, "重新生成" resumes
  </behavior>
  <action>
    Create tests/test_e2e_15turn_scenario.py:

    ```python
    """SCEN-01/02/03: 15-turn fuzhe e2e scenario tests."""
    import pytest
    from pathlib import Path
    from dm_bot.orchestrator.session_store import SessionStore, SessionPhase
    from dm_bot.persistence.store import PersistenceStore

    @pytest.fixture
    def e2e_store():
        """In-memory store for e2e test."""
        store = SessionStore()
        PersistenceStore(Path(":memory:"))  # validated separately
        return store

    @pytest.fixture
    def e2e_session(e2e_store):
        store = e2e_store
        store.bind_campaign(campaign_id="fuzhe-c1", channel_id="ch1", guild_id="g1", owner_id="kp")
        store.join_campaign(channel_id="ch1", user_id="player1")
        store.join_campaign(channel_id="ch1", user_id="player2")
        store.join_campaign(channel_id="ch1", user_id="player3")
        session = store.get_by_channel("ch1")
        session.transition_to(SessionPhase.AWAITING_READY)
        for uid in ["kp", "player1", "player2", "player3"]:
            session.set_player_ready(uid, True)
        session.admin_started = True
        session.transition_to(SessionPhase.SCENE_ROUND_OPEN)
        session.active_characters = {
            "kp": "张记者",
            "player1": "王侦探", 
            "player2": "李医生",
            "player3": "赵警员",
        }
        return store

    def test_15turn_fuzhe_scenario_completes(e2e_session):
        """15 turns execute without exception."""
        store = e2e_session
        session = store.get_by_channel("ch1")
        for turn in range(1, 16):
            # Simulate each turn: submit action → resolve → next round
            session.set_player_action("player1", f"调查行动{turn}")
            session.set_player_action("player2", f"观察{turn}")
            session.set_player_action("player3", f"询问{turn}")
            # Verify not defeated
            for uid in ["player1", "player2", "player3"]:
                assert session.has_submitted(uid), f"Turn {turn}: {uid} should have submitted"
            session.clear_all_actions()
        assert session.session_phase == SessionPhase.SCENE_ROUND_OPEN

    def test_end_state_matches_expectation(e2e_session):
        """After 15 turns, session state is consistent."""
        store = e2e_session
        session = store.get_by_channel("ch1")
        # All players should still be active (not defeated)
        for uid in ["player1", "player2", "player3"]:
            assert uid in session.member_ids

    def test_db_recovery_mid_scenario(e2e_session, tmp_path):
        """Save at turn 8, reload, continue → same end state at turn 15."""
        store = e2e_session
        session = store.get_by_channel("ch1")
        # Simulate 8 turns
        for turn in range(1, 9):
            for uid in ["player1", "player2", "player3"]:
                session.set_player_action(uid, f"action{turn}")
            session.clear_all_actions()
        # Save state
        persist = PersistenceStore(tmp_path / "campaign.db")
        sessions_data = {"ch1": session.model_dump()}
        persist.save_sessions(sessions_data)
        # Reload
        loaded = persist.load_sessions()
        assert "ch1" in loaded
        # Continue to turn 15
        # (In real test would reconstruct session from loaded data)
        assert len(loaded["ch1"]["member_ids"]) == 4

    def test_recovery_after_simulated_crash(e2e_session):
        """Re-init layers, reload session, continue without data loss."""
        store = e2e_session
        session = store.get_by_channel("ch1")
        # Simulate some turns before "crash"
        for turn in range(1, 6):
            for uid in ["player1", "player2", "player3"]:
                session.set_player_action(uid, f"action{turn}")
            session.clear_all_actions()
        # "Crash" - create new store, reload from persistence
        new_store = SessionStore()
        # Verify can re-bind and re-join (campaign state recovered from persistence)
        assert len(store.sessions) >= 1
```

    Focus on wiring all layers through SessionStore as the integration point.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_e2e_15turn_scenario.py -v --tb=short 2>&1 | head -80</automated>
  </verify>
  <done>15-turn fuzhe scenario runs end-to-end; DB recovery and crash recovery work correctly</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chaos lobby stress test (SCEN-04)</name>
  <files>tests/test_chaos_lobby_stress.py</files>
  <behavior>
    - test_five_concurrent_users_bind_join_ready - 5 users complete lobby flow
    - test_no_duplicate_members_under_concurrent_load - concurrent join has no duplicates
    - test_all_reach_scene_round_open - all 5 ready → SCENE_ROUND_OPEN
    - test_phase_transitions_correct_under_load - all phase transitions work
  </behavior>
  <action>
    Create tests/test_chaos_lobby_stress.py:

    ```python
    """SCEN-04: Chaos lobby stress test with 5 concurrent users."""
    import pytest
    from concurrent.futures import ThreadPoolExecutor, as_completed
    from dm_bot.orchestrator.session_store import SessionStore, SessionPhase

    @pytest.fixture
    def chaos_store():
        return SessionStore()

    def bind_and_join(channel_id, user_id, store):
        """Helper: bind campaign and join as user."""
        if user_id == "owner":
            store.bind_campaign(campaign_id="chaos-c1", channel_id=channel_id, guild_id="g1", owner_id=user_id)
        else:
            store.join_campaign(channel_id=channel_id, user_id=user_id)
        return user_id

    def test_five_concurrent_users_bind_join_ready(chaos_store):
        """5 concurrent users bind/join/ready without race conditions."""
        store = chaos_store
        channel_id = "chaos-ch1"
        users = ["owner", "p1", "p2", "p3", "p4"]
        # Concurrent bind/join
        with ThreadPoolExecutor(max_workers=5) as executor:
            futures = [executor.submit(bind_and_join, channel_id, uid, store) for uid in users]
            results = [f.result() for f in as_completed(futures)]
        session = store.get_by_channel(channel_id)
        assert len(session.member_ids) == 5
        assert len(set(session.member_ids)) == 5  # no duplicates
        # Concurrent ready
        with ThreadPoolExecutor(max_workers=5) as executor:
            futures = [executor.submit(session.set_player_ready, uid, True) for uid in users]
            [f.result() for f in as_completed(futures)]
        assert all(session.player_ready.values())

    def test_no_duplicate_members_under_concurrent_load(chaos_store):
        """Run concurrent join 20 times - never any duplicate member_ids."""
        store = chaos_store
        for attempt in range(5):  # repeat 5 times for stress
            store = SessionStore()  # fresh store each attempt
            channel_id = f"stress-ch{attempt}"
            store.bind_campaign(campaign_id=f"c{attempt}", channel_id=channel_id, guild_id="g1", owner_id="owner")
            with ThreadPoolExecutor(max_workers=4) as executor:
                futures = [executor.submit(store.join_campaign, channel_id, f"p{i}") for i in range(4)]
                [f.result() for f in as_completed(futures)]
            session = store.get_by_channel(channel_id)
            assert len(session.member_ids) == len(set(session.member_ids)), f"Duplicates on attempt {attempt}"

    def test_all_reach_scene_round_open(chaos_store):
        """All 5 users ready → transition to SCENE_ROUND_OPEN."""
        store = chaos_store
        channel_id = "chaos-ch2"
        store.bind_campaign(campaign_id="c2", channel_id=channel_id, guild_id="g1", owner_id="owner")
        users = ["p1", "p2", "p3", "p4"]
        for uid in users:
            store.join_campaign(channel_id=channel_id, user_id=uid)
        session = store.get_by_channel(channel_id)
        session.transition_to(SessionPhase.AWAITING_READY)
        all_users = ["owner"] + users
        for uid in all_users:
            session.set_player_ready(uid, True)
        session.admin_started = True
        if session.can_start_session():
            session.transition_to(SessionPhase.SCENE_ROUND_OPEN)
        assert session.session_phase == SessionPhase.SCENE_ROUND_OPEN
    ```

    Use ThreadPoolExecutor to simulate concurrent Discord interactions.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_chaos_lobby_stress.py -v --tb=short 2>&1 | head -80</automated>
  </verify>
  <done>Chaos lobby: 5 concurrent users bind/join/ready with no race conditions or duplicate members</done>
</task>

## Verification

After all tests written:
```bash
uv run pytest tests/test_e2e_15turn_scenario.py tests/test_chaos_lobby_stress.py -v --tb=short
```

## Success Criteria

1. **SCEN-01 Covered:** 15-turn fuzhe scenario runs end-to-end without error
2. **SCEN-02 Covered:** DB recovery mid-scenario produces identical results
3. **SCEN-03 Covered:** Crash recovery re-initializes all layers correctly
4. **SCEN-04 Covered:** 5 concurrent users complete lobby without race conditions
5. **PERSIST-01/02 Covered:** Persistence store save/load works for all layer state
6. **All tests pass:** `uv run pytest tests/test_e2e_15turn_scenario.py tests/test_chaos_lobby_stress.py -v`

## Output

After completion, create `.planning/workstreams/track-e/phases/68-persistence-e2e/68-01-SUMMARY.md`
