# Plan 41-01: CLI Control Surface

## Goal

Expose the runtime control service through stable CLI entry points so operators can inspect and control the local runtime without guessing from Discord behavior.

## Delivered

- Added `control-status` entry point
- Added `run-control-panel` entry point
- Kept `restart-system` and `smoke-check` as the operational backbone

## Verification

- `uv run pytest tests/test_main_runtime.py -q`
- `uv run python -m dm_bot.main control-status`
