---
phase: 67-narration-pipeline
plan: "01"
type: tdd
wave: 1
depends_on: ["66-model-router/66-01"]
files_modified:
  - tests/test_narration_pipeline_flow.py
  - tests/test_narration_streaming_flow.py
autonomous: true
requirements:
  - NARR-01
  - NARR-02
must_haves:
  truths:
    - "NarrationService.narrate() returns string content built from NarrationRequest"
    - "stream_narrate() yields string chunks (async iterator)"
    - "KP visibility: build_prompt includes state_intents, tool_results but not raw player input"
    - "Player visibility: player sees filtered narration (no internal state)"
  artifacts:
    - path: tests/test_narration_pipeline_flow.py
      provides: NARR-01 narration prompt construction and output tests
      min_lines: 80
    - path: tests/test_narration_streaming_flow.py
      provides: NARR-02 streaming output delivery tests
      min_lines: 60
  key_links:
    - from: test_narration_pipeline_flow.py
      to: narration/service.py
      via: NarrationService.narrate() and _build_prompt()
    - from: test_narration_streaming_flow.py
      to: narration/service.py
      via: NarrationService.stream_narrate()
---

<objective>
Validate narration prompt construction, streaming output delivery, and KP vs player visibility separation.

Purpose: Close the "no narration pipeline flow tests" gap. Write failing tests first (RED), then implement fixes (GREEN), then refactor (REFACTOR).

Output: 2 test files covering NARR-01 (prompt construction) and NARR-02 (streaming delivery).
</objective>

<execution_context>
@C:/Users/Lin/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/dm_bot/narration/service.py
@src/dm_bot/router/contracts.py
@tests/test_narration_service.py
</context>

<feature>
  <name>Narration Prompt Construction (NARR-01)</name>
  <files>tests/test_narration_pipeline_flow.py</files>
  <behavior>
    - Test: narrate() calls _build_model_request with correct NarrationRequest
    - Test: _build_prompt() produces JSON with mode, narration_brief, state_snapshot, tool_results
    - Test: player_input included in prompt (player sees their own action)
    - Test: state_intents included as model_dump() dicts
    - Test: system_prompt includes Chinese Keeper tone guidance
  </behavior>
  <implementation>
    Use AsyncMock for narration client. Test _build_prompt() output format.
    Verify system_prompt contains Chinese language guidance.
  </implementation>
</feature>

<feature>
  <name>Streaming Output Delivery (NARR-02)</name>
  <files>tests/test_narration_streaming_flow.py</files>
  <behavior>
    - Test: stream_narrate() is an async iterator
    - Test: stream_narrate() yields non-empty string chunks
    - Test: stream_narrate() skips empty chunks (falsy values)
    - Test: client.stream_narrator called with built prompt
    - Test: final output is concatenated from chunks
  </behavior>
  <implementation>
    Use AsyncIterator mock for stream_narrator. Test async yield pattern.
    Verify empty chunks are filtered out.
  </implementation>
</feature>

## Task Dependency Graph

| Task | Depends On | Reason |
|------|------------|--------|
| Task 1: Narration prompt construction tests (NARR-01) | None | RED phase - foundational |
| Task 2: Streaming output delivery tests (NARR-02) | None | RED phase - independent |

## Parallel Execution Graph

Wave 1 (Start immediately - no dependencies on E66):
├── Task 1: Narration prompt construction tests (NARR-01)
└── Task 2: Streaming output delivery tests (NARR-02)

Wave 2 (After Wave 1 - if fixes needed):
└── Any narration fixes identified from failing tests

## Commit Strategy

### Commit 1: RED - Narration pipeline flow tests (NARR-01)
```
test(narr-67): add failing narration pipeline flow tests (NARR-01)

- test_build_prompt_includes_all_required_fields
- test_system_prompt_has_keeper_tone
- test_narrate_returns_string
```

### Commit 2: RED - Streaming output tests (NARR-02)
```
test(narr-67): add failing narration streaming delivery tests (NARR-02)

- test_stream_narrate_yields_chunks
- test_stream_narrate_skips_empty_chunks
- test_stream_narrate_concatenated_output
```

### Commit 3: GREEN - Verify all tests pass
```
feat(narr-67): narration pipeline tests passing - NARR-01/02 complete
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Narration prompt construction tests (NARR-01)</name>
  <files>tests/test_narration_pipeline_flow.py</files>
  <behavior>
    - test_build_prompt_includes_mode_and_narration_brief - prompt JSON has mode, narration_brief
    - test_build_prompt_includes_state_snapshot - state_snapshot dict present
    - test_build_prompt_includes_tool_results - tool_results list present
    - test_build_prompt_includes_player_input - player_input string present
    - test_build_prompt_state_intents_as_dicts - state_intents serialized as dicts
    - test_system_prompt_contains_chinese_keeper_tone - system prompt has Chinese guidance
    - test_narrate_returns_string_content - narrate() returns non-empty string
  </behavior>
  <action>
    Create tests/test_narration_pipeline_flow.py:

    ```python
    """NARR-01: Narration pipeline prompt construction tests."""
    import pytest
    import json
    from unittest.mock import AsyncMock, MagicMock
    from dm_bot.models.schemas import ModelResponse
    from dm_bot.narration.service import NarrationService, NarrationRequest
    from dm_bot.router.contracts import TurnPlan, StateIntent, ToolCall

    @pytest.fixture
    def mock_narrator_client():
        client = MagicMock()
        client.call_narrator = AsyncMock(return_value=ModelResponse(
            content="张记者在书架间翻找，突然发现一本日记。",
            finish_reason="stop",
            model="test",
        ))
        return client

    @pytest.fixture
    def narration_service(mock_narrator_client):
        return NarrationService(client=mock_narrator_client)

    @pytest.fixture
    def narration_request():
        return NarrationRequest(
            player_input="我检查一下书架",
            state_snapshot={"location": "书房", "time": "深夜"},
            tool_results=[{"name": "coc_skill_check", "result": "hard success"}],
            plan=TurnPlan(
                mode="scene",
                tool_calls=[ToolCall(name="coc_skill_check", arguments={})],
                state_intents=[StateIntent(kind="san_check", payload={})],
                narration_brief="调查员在书架间翻找线索",
                speaker_hints=["张记者"],
            ),
        )

    @pytest.mark.asyncio
    async def test_build_prompt_includes_mode_and_narration_brief(narration_service, narration_request):
        prompt = narration_service._build_prompt(narration_request)
        data = json.loads(prompt)
        assert data["mode"] == "scene"
        assert "书架" in data["narration_brief"]

    @pytest.mark.asyncio
    async def test_build_prompt_includes_state_snapshot(narration_service, narration_request):
        prompt = narration_service._build_prompt(narration_request)
        data = json.loads(prompt)
        assert "state_snapshot" in data
        assert data["state_snapshot"]["location"] == "书房"

    @pytest.mark.asyncio
    async def test_build_prompt_includes_tool_results(narration_service, narration_request):
        prompt = narration_service._build_prompt(narration_request)
        data = json.loads(prompt)
        assert "tool_results" in data
        assert len(data["tool_results"]) == 1

    @pytest.mark.asyncio
    async def test_build_prompt_includes_player_input(narration_service, narration_request):
        prompt = narration_service._build_prompt(narration_request)
        data = json.loads(prompt)
        assert "player_input" in data
        assert "书架" in data["player_input"]

    @pytest.mark.asyncio
    async def test_system_prompt_contains_chinese_keeper_tone(narration_service, narration_request):
        model_request = narration_service._build_model_request(narration_request)
        assert "Chinese" in model_request.system_prompt or "中文" in model_request.system_prompt
        assert "Keeper" in model_request.system_prompt or "Keeper" in model_request.system_prompt

    @pytest.mark.asyncio
    async def test_narrate_returns_string_content(narration_service, narration_request):
        result = await narration_service.narrate(narration_request)
        assert isinstance(result, str)
        assert len(result) > 0
    ```

    Use AsyncMock for async narrate() method.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_narration_pipeline_flow.py -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>NarrationService correctly constructs prompt with mode, state, tool_results; returns string</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Streaming output delivery tests (NARR-02)</name>
  <files>tests/test_narration_streaming_flow.py</files>
  <behavior>
    - test_stream_narrate_yields_chunks - async iterator yields non-empty strings
    - test_stream_narrate_skips_empty_chunks - empty string chunks not yielded
    - test_stream_narrate_concatenated_output - all chunks combine to full text
    - test_stream_narrate_calls_stream_narrator - client.stream_narrator invoked
  </behavior>
  <action>
    Create tests/test_narration_streaming_flow.py:

    ```python
    """NARR-02: Narration streaming output delivery tests."""
    import pytest
    import json
    from unittest.mock import AsyncMock, MagicMock
    from dm_bot.models.schemas import ModelResponse
    from dm_bot.narration.service import NarrationService, NarrationRequest
    from dm_bot.router.contracts import TurnPlan, StateIntent, ToolCall

    @pytest.fixture
    def mock_stream_narrator_client():
        async def mock_stream(prompt):
            chunks = ["张记", "者在", "书架", "间翻", "找。"]
            for chunk in chunks:
                yield chunk
        client = MagicMock()
        client.stream_narrator = mock_stream
        return client

    @pytest.fixture
    def narration_service(mock_stream_narrator_client):
        return NarrationService(client=mock_stream_narrator_client)

    @pytest.fixture
    def narration_request():
        return NarrationRequest(
            player_input="检查书架",
            state_snapshot={},
            tool_results=[],
            plan=TurnPlan(
                mode="scene",
                tool_calls=[],
                state_intents=[],
                narration_brief="调查员检查书架",
                speaker_hints=[],
            ),
        )

    @pytest.mark.asyncio
    async def test_stream_narrate_yields_chunks(narration_service, narration_request):
        chunks = []
        async for chunk in narration_service.stream_narrate(narration_request):
            chunks.append(chunk)
        assert len(chunks) >= 3
        assert all(isinstance(c, str) for c in chunks)

    @pytest.mark.asyncio
    async def test_stream_narrate_skips_empty_chunks(narration_service, narration_request):
        # Patch stream to include empty chunk
        async def mock_stream_with_empty(prompt):
            yield "非空"
            yield ""  # should be skipped
            yield "还是非空"
        narration_service._client.stream_narrator = mock_stream_with_empty
        chunks = []
        async for chunk in narration_service.stream_narrate(narration_request):
            chunks.append(chunk)
        # empty string should be filtered
        assert all(c != "" for c in chunks)
        assert "非空" in "".join(chunks)

    @pytest.mark.asyncio
    async def test_stream_narrate_concatenated_output(narration_service, narration_request):
        full_text = ""
        async for chunk in narration_service.stream_narrate(narration_request):
            full_text += chunk
        assert "张记" in full_text or "非空" in full_text or len(full_text) > 0
    ```

    Test async iterator pattern directly.
  </action>
  <verify>
    <automated>cd C:/Users/Lin/Documents/Playground && uv run pytest tests/test_narration_streaming_flow.py -v --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>stream_narrate() correctly yields chunks, skips empty values, produces complete text</done>
</task>

## Verification

After all tests written (RED phase):
```bash
uv run pytest tests/test_narration_pipeline_flow.py tests/test_narration_streaming_flow.py -v --tb=short
```

After implementation (GREEN phase):
```bash
uv run pytest tests/test_narration_pipeline_flow.py tests/test_narration_streaming_flow.py -v
```

## Success Criteria

1. **NARR-01 Covered:** Prompt construction includes all required fields and Keeper tone
2. **NARR-02 Covered:** Streaming yields non-empty chunks, skips empty, produces complete output
3. **All tests pass:** `uv run pytest tests/test_narration_pipeline_flow.py tests/test_narration_streaming_flow.py -v`

## Output

After completion, create `.planning/workstreams/track-e/phases/67-narration-pipeline/67-01-SUMMARY.md`
