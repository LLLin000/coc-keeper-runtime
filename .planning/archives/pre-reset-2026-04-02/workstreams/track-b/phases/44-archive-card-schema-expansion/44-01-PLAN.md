# Plan 44-01

## Step 1
Expand `InvestigatorArchiveProfile` with richer long-lived card sections and keep defaults nullable for backward compatibility.

## Step 2
Update archive detail presentation so the new sections are visible and clearly labeled as long-lived archive truth.

## Step 3
Add tests covering new schema defaults, richer detail output, and non-leakage of campaign-local state.
