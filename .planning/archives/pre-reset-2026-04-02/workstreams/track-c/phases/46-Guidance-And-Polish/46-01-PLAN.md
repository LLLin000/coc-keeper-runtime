---
phase: 46-Guidance-And-Polish
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - src/dm_bot/discord_bot/commands.py
autonomous: true
requirements:
  - GUIDE-03
  - CLUTTER-01
  - CLUTTER-02
  - CLUTTER-03

must_haves:
  truths:
    - New users get welcome message with channel structure explanation
    - Long command outputs use ephemeral or follow-up modes
    - Diagnostic commands prefer trace/admin channels
    - Gameplay narration stays in game halls
  artifacts:
    - updates: Welcome message with channel structure
---

<objective>
Add user guidance and reduce command clutter in game halls.

Purpose: Improve user experience with better onboarding and cleaner channel usage.
Output: Welcome messages, command output improvements, and diagnostics channel discipline.
</objective>

<context>
@C:/Users/Lin/Documents/Playground/src/dm_bot/discord_bot/commands.py
@C:/Users/Lin/Documents/Playground/src/dm_bot/discord_bot/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add welcome message with channel structure</name>
  <files>src/dm_bot/discord_bot/client.py</files>
  <action>
    Add a welcome message or setup command that explains channel structure:
    - When bot joins a guild or first used, show channel setup guidance
    - Explain the 4 channel types: 角色档案, 游戏大厅, KP-trace, 管理员
    - Provide instructions for binding each channel type
    
    Alternatively, enhance /setup_check to show channel guidance:
    - Show current bindings (archive, game, admin, trace)
    - Show how to bind each channel type
  </action>
  <verify>
    <manual>Run /setup_check or welcome - should show channel structure</manual>
  </verify>
  <done>Welcome/setup shows channel structure</done>
</task>

<task type="auto">
  <name>Task 2: Ensure long outputs use ephemeral mode</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    Review command outputs and ensure:
    - Long list outputs (like /profiles with many items) use ephemeral=True
    - Detailed command outputs (/profile_detail) use ephemeral=True
    - Only short confirmation messages are visible to all
    
    Check existing commands:
    - list_profiles already uses ephemeral=True
    - profile_detail already uses ephemeral=True
    - Add ephemeral=True to any commands missing it for long outputs
  </action>
  <verify>
    <manual>Check command responses - long outputs should be ephemeral</manual>
  </verify>
  <done>Long outputs use ephemeral mode</done>
</task>

<task type="auto">
  <name>Task 3: Ensure diagnostic commands prefer trace/admin channels</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    Review diagnostic/debug commands and ensure they:
    - Prefer ephemeral mode (private to user)
    - If visible output, suggest using trace/admin channel
    
    Check existing commands:
    - setup_check already uses ephemeral=True
    - Other diagnostics should follow same pattern
  </action>
  <verify>
    <manual>Check diagnostic commands - should prefer ephemeral or trace/admin</manual>
  </verify>
  <done>Diagnostic commands follow channel discipline</done>
</task>

<task type="auto">
  <name>Task 4: Ensure gameplay narration stays in game halls</name>
  <files>src/dm_bot/discord_bot/commands.py</files>
  <action>
    Verify that gameplay narration (from take_turn responses) only goes to game channels:
    - Already enforced by game channel binding
    - Ensure no cross-posting to other channels
    
    This is already implemented - verify it works correctly
  </action>
  <verify>
    <manual>Play a turn - narration should only appear in game channel</manual>
  </verify>
  <done>Gameplay narration stays in game halls</done>
</task>

</tasks>

<verification>
- [ ] Welcome/setup shows channel structure explanation
- [ ] Long command outputs use ephemeral mode
- [ ] Diagnostic commands prefer trace/admin channels or ephemeral
- [ ] Gameplay narration stays in game halls
</verification>

<success_criteria>
1. GUIDE-03: New users get welcome message with channel structure
2. CLUTTER-01: Long command outputs use ephemeral mode
3. CLUTTER-02: Diagnostic commands prefer trace/admin channels
4. CLUTTER-03: Gameplay narration stays in game halls
</success_criteria>

<output>
After completion, create `.planning/workstreams/track-c/phases/46-Guidance-And-Polish/46-01-SUMMARY.md`
</output>
