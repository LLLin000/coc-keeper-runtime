---
phase: "01"
plan: "02"
type: "standard"
wave: 3
depends_on:
  - "01"
  - "03"
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - OPS-03
files_modified:
  - src/dm_bot/discord_bot/client.py
  - src/dm_bot/discord_bot/commands.py
  - src/dm_bot/orchestrator/turns.py
  - src/dm_bot/orchestrator/session_store.py
  - src/dm_bot/runtime/app.py
  - src/dm_bot/main.py
  - tests/test_turns.py
  - tests/test_commands.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Operators can run setup or health workflows before live play."
    - "A Discord session can be bound to a channel or thread using explicit commands."
    - "Campaign-scoped actions are serialized and defer long work instead of blocking the Discord interaction window."
  artifacts:
    - path: "src/dm_bot/discord_bot/client.py"
      exists: true
    - path: "src/dm_bot/discord_bot/commands.py"
      exists: true
    - path: "src/dm_bot/orchestrator/turns.py"
      exists: true
    - path: "src/dm_bot/orchestrator/session_store.py"
      exists: true
    - path: "tests/test_turns.py"
      exists: true
  key_links:
    - from: "src/dm_bot/discord_bot/commands.py"
      to: "src/dm_bot/orchestrator/session_store.py"
      via: "commands create or bind campaign sessions to Discord channel/thread context"
    - from: "src/dm_bot/discord_bot/commands.py"
      to: "src/dm_bot/orchestrator/turns.py"
      via: "commands hand off turn requests through asynchronous orchestration"
    - from: "src/dm_bot/orchestrator/turns.py"
      to: "src/dm_bot/orchestrator/turn_runner.py"
      via: "serialized Discord turns delegate into the dual-model runner"
---

# Phase 01 Plan 02: Discord Session Surface and Turn-Runner Integration

<objective>
Add the Discord-facing session commands, campaign-bound interaction flow, and serialized asynchronous handoff into the dual-model turn runner needed to make the runtime responsive and safe for multiple players.
</objective>

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

## Tasks

<task type="auto">
  <name>Implement Discord client and command surface for campaign setup</name>
  <read_first>
    <file>.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md</file>
    <file>src/dm_bot/config.py</file>
    <file>src/dm_bot/runtime/app.py</file>
  </read_first>
  <action>Add `src/dm_bot/discord_bot/client.py` and `src/dm_bot/discord_bot/commands.py` using `discord.py` application commands. Implement a minimal command set for health/setup verification and campaign binding in a channel or thread, such as a setup check command, a bind or create session command, and a join or inspect session command. Ensure the command surface uses structured interactions rather than freeform message parsing as the canonical entry path.</action>
  <files>src/dm_bot/discord_bot/client.py, src/dm_bot/discord_bot/commands.py</files>
  <verify>
    <automated>uv run pytest tests/test_commands.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/dm_bot/discord_bot/commands.py` defines app-command handlers for a setup or health check and a campaign bind/create flow.</criterion>
    <criterion>The command layer reads runtime settings from the shared config module instead of duplicating environment handling.</criterion>
    <criterion>`src/dm_bot/discord_bot/client.py` initializes a Discord client or bot object suitable for app-command registration.</criterion>
  </acceptance_criteria>
  <done>The runtime exposes a concrete Discord app-command surface for setup and session management without relying on freeform messages.</done>
</task>

<task type="auto">
  <name>Implement campaign-scoped session registry and serialized turn pipeline</name>
  <read_first>
    <file>.planning/research/ARCHITECTURE.md</file>
    <file>.planning/research/PITFALLS.md</file>
    <file>src/dm_bot/models/schemas.py</file>
    <file>src/dm_bot/models/ollama_client.py</file>
  </read_first>
  <action>Add `src/dm_bot/orchestrator/session_store.py` for in-memory or lightweight runtime session binding and `src/dm_bot/orchestrator/turns.py` for campaign-scoped turn serialization. Implement a turn envelope that captures Discord context, campaign identity, and trace IDs, and ensure long-running operations acknowledge quickly and execute in a deferred or follow-up style instead of trying to finish inside the initial interaction response. This turn layer must delegate model-facing orchestration to `src/dm_bot/orchestrator/turn_runner.py` from Plan 03 instead of calling the model transport directly, so the command path is explicitly wired through the dual-model boundary.</action>
  <files>src/dm_bot/orchestrator/session_store.py, src/dm_bot/orchestrator/turns.py, tests/test_turns.py</files>
  <verify>
    <automated>uv run pytest tests/test_turns.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/dm_bot/orchestrator/session_store.py` can bind a campaign session to a Discord channel or thread identity.</criterion>
    <criterion>`src/dm_bot/orchestrator/turns.py` contains serialization logic keyed by campaign or thread identity.</criterion>
    <criterion>The turn pipeline creates or propagates a stable trace identifier for each inbound action.</criterion>
    <criterion>`src/dm_bot/orchestrator/turns.py` delegates orchestration work to `src/dm_bot/orchestrator/turn_runner.py`.</criterion>
  </acceptance_criteria>
  <done>The command surface can bind sessions and hand off deferred, campaign-scoped turns into `turn_runner.py` through a serialized orchestration path.</done>
</task>

<task type="auto">
  <name>Wire runtime entrypoint and add behavioral tests</name>
  <read_first>
    <file>src/dm_bot/runtime/app.py</file>
    <file>src/dm_bot/discord_bot/commands.py</file>
    <file>src/dm_bot/orchestrator/turns.py</file>
  </read_first>
  <action>Add `src/dm_bot/main.py` or equivalent runtime entrypoint that can start the FastAPI service and Discord bot according to configuration. Extend tests with `tests/test_turns.py` and `tests/test_commands.py` to verify campaign binding behavior, serialized turn acceptance, deferred response behavior, and setup-check command wiring. Keep tests local and deterministic by mocking Discord/Ollama boundaries instead of requiring live external services.</action>
  <files>src/dm_bot/runtime/app.py, src/dm_bot/main.py, tests/test_turns.py, tests/test_commands.py</files>
  <verify>
    <automated>uv run pytest tests/test_turns.py tests/test_commands.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/dm_bot/main.py` exists and wires together the runtime service and Discord bot startup path.</criterion>
    <criterion>`tests/test_turns.py` verifies serialized campaign-scoped turn handling and delegation into `turn_runner.py`.</criterion>
    <criterion>`tests/test_commands.py` verifies session command wiring or handler behavior without requiring live Discord access.</criterion>
  </acceptance_criteria>
  <done>The runtime has a local startup path and behavioral tests that cover commands, deferred handling, and the handoff into `turn_runner.py`.</done>
</task>

## Verification
- Run the full Phase 1 test suite and confirm all tests pass.
- Confirm the command layer can create or bind a session and hand off a mocked turn through the orchestrator.
- Confirm a mocked Discord command path flows through `commands.py -> turns.py -> turn_runner.py`.
- Confirm the turn path uses deferred/follow-up behavior rather than synchronous long-running replies.

## Success Criteria
- [ ] Discord app-command surface exists for setup and campaign session binding
- [ ] Campaign-scoped turn serialization and handoff into `turn_runner.py` exist and are covered by tests
- [ ] Router/narrator flow is reachable through the orchestrator without making the narrator authoritative
- [ ] Runtime entrypoint is wired for local startup
