---
phase: "01"
plan: "03"
type: "standard"
wave: 2
depends_on:
  - "01"
files_modified:
  - src/dm_bot/models/ollama_client.py
  - src/dm_bot/router/contracts.py
  - src/dm_bot/router/service.py
  - src/dm_bot/narration/service.py
  - src/dm_bot/orchestrator/turn_runner.py
  - src/dm_bot/runtime/model_checks.py
  - tests/test_dual_model_orchestration.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - OPS-03
must_haves:
  truths:
    - "The runtime uses a small router model and a separate narrator model."
    - "Router output is structured intent data instead of final DM prose."
    - "Narration uses compact context and validated tool results without mutating canonical state."
    - "Model setup and reachability are visible in the health workflow."
  artifacts:
    - path: "src/dm_bot/router/contracts.py"
      provides: "Pydantic schemas for structured routing output."
    - path: "src/dm_bot/router/service.py"
      provides: "Router model invocation and validation logic."
    - path: "src/dm_bot/narration/service.py"
      provides: "Narrator model invocation with compact context input."
    - path: "src/dm_bot/orchestrator/turn_runner.py"
      provides: "Turn execution pipeline joining router and narrator without state mutation leakage."
    - path: "tests/test_dual_model_orchestration.py"
      provides: "Automated verification for structured routing and narration boundaries."
  key_links:
    - from: "src/dm_bot/orchestrator/turn_runner.py"
      to: "src/dm_bot/router/service.py"
      via: "validated TurnPlan call"
      pattern: "TurnPlan|route"
    - from: "src/dm_bot/orchestrator/turn_runner.py"
      to: "src/dm_bot/narration/service.py"
      via: "compact narration payload"
      pattern: "narrat|speaker|context"
    - from: "src/dm_bot/runtime/model_checks.py"
      to: "src/dm_bot/models/ollama_client.py"
      via: "model readiness check"
      pattern: "router|narrator|ollama"
---

<objective>
Implement the local dual-model control loop: structured router contracts, separate narrator integration, and orchestration boundaries that keep canonical state outside model prose.

Purpose: Deliver the model-control half of Phase 1 per D-07, D-08, D-09, and D-12 so later gameplay work can plug into a safe router-plus-narrator pipeline.
Output: Ollama-compatible model client, structured router schemas and service, narrator service, turn runner pipeline, and model-focused health checks with tests.
</objective>

<execution_context>
@C:/Users/Lin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Lin/Documents/Playground/CLAUDE.md
@C:/Users/Lin/Documents/Playground/.planning/ROADMAP.md
@C:/Users/Lin/Documents/Playground/.planning/REQUIREMENTS.md
@C:/Users/Lin/Documents/Playground/.planning/phases/01-discord-runtime-dual-model-control/01-CONTEXT.md
@C:/Users/Lin/Documents/Playground/.planning/phases/01-discord-runtime-dual-model-control/01-01-PLAN.md
@C:/Users/Lin/Documents/Playground/.planning/research/STACK.md
@C:/Users/Lin/Documents/Playground/.planning/research/ARCHITECTURE.md
@C:/Users/Lin/Documents/Playground/.planning/research/PITFALLS.md

<interfaces>
Build against Plan 01 settings and add these new contracts in `src/dm_bot/router/contracts.py`:

```python
from pydantic import BaseModel
from typing import Literal

class ToolCall(BaseModel):
    name: str
    arguments: dict[str, object]

class StateIntent(BaseModel):
    kind: str
    payload: dict[str, object]

class TurnPlan(BaseModel):
    mode: Literal["dm", "scene", "combat"]
    tool_calls: list[ToolCall]
    state_intents: list[StateIntent]
    narration_brief: str
```

Keep router output structured per D-08. Final prose belongs only to the narrator service.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Ollama-compatible dual-model clients and router contracts</name>
  <read_first>
    - C:/Users/Lin/Documents/Playground/.planning/research/STACK.md
    - C:/Users/Lin/Documents/Playground/.planning/research/PITFALLS.md
    - C:/Users/Lin/Documents/Playground/.planning/phases/01-discord-runtime-dual-model-control/01-01-PLAN.md
  </read_first>
  <files>src/dm_bot/models/ollama_client.py, src/dm_bot/router/contracts.py, src/dm_bot/router/service.py, tests/test_dual_model_orchestration.py</files>
  <action>Build on the shared Ollama transport and settings created in Plan 01 instead of recreating them. In `router/contracts.py`, define strict Pydantic output models for structured route decisions, tool requests, and state intents, then implement `router/service.py` so the router model returns validated JSON rather than final prose per D-08. Reject malformed router payloads hard instead of trying to salvage them.</action>
  <verify>
    <automated>uv run pytest tests/test_dual_model_orchestration.py -k "router or contract" -q</automated>
  </verify>
  <acceptance_criteria>The router is configured as a separate model endpoint or model name, its output is schema-validated, and invalid structured output is surfaced as an orchestration failure rather than a silent state guess.</acceptance_criteria>
  <done>The runtime has a working dual-model client boundary and a typed router contract aligned with ORCH-01 and ORCH-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire router-to-narrator orchestration and model health checks</name>
  <read_first>
    - C:/Users/Lin/Documents/Playground/.planning/research/ARCHITECTURE.md
    - C:/Users/Lin/Documents/Playground/.planning/research/PITFALLS.md
    - C:/Users/Lin/Documents/Playground/.planning/phases/01-discord-runtime-dual-model-control/01-01-PLAN.md
  </read_first>
  <files>src/dm_bot/narration/service.py, src/dm_bot/orchestrator/turn_runner.py, src/dm_bot/runtime/model_checks.py, tests/test_dual_model_orchestration.py</files>
  <action>Implement a turn runner that consumes validated router output, accepts compact tool results or placeholder deterministic results, and hands only compact narration context to the narrator per D-09. The narrator service must return final Discord-ready prose but never apply state writes. Add model-specific setup or health checks so operators can verify router and narrator availability, configured model names, and connectivity from the preflight workflow created in Plan 01.</action>
  <verify>
    <automated>uv run pytest tests/test_dual_model_orchestration.py -q</automated>
  </verify>
  <acceptance_criteria>The runtime can call the router and narrator as separate concerns, preserve the router as the source of structured mode or tool intent, and expose model readiness in the basic setup workflow.</acceptance_criteria>
  <done>ORCH-03 and ORCH-04 are satisfied by a compact-context narrator pipeline, distinct router and narrator checks, and explicit non-authoritative narration boundaries.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_dual_model_orchestration.py -q`. Confirm the tests cover schema validation, router failure handling, distinct model configuration for router and narrator, compact narration inputs, and health reporting for both model slots.
</verification>

<success_criteria>
- The router and narrator are separate configurable model integrations.
- Router output is structured and validated before downstream processing.
- Narration consumes compact context and cannot mutate canonical state directly.
- Operators can see router and narrator readiness in the setup workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discord-runtime-dual-model-control/01-03-SUMMARY.md`
</output>
