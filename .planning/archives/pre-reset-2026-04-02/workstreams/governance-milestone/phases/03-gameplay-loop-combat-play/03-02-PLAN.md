---
phase: "03"
plan: "02"
type: "standard"
wave: 2
depends_on:
  - "01"
files_modified:
  - src/dm_bot/gameplay/combat.py
  - src/dm_bot/rules/actions.py
  - src/dm_bot/rules/engine.py
  - tests/test_combat_loop.py
autonomous: true
requirements:
  - RULE-02
  - RULE-03
must_haves:
  truths:
    - "Combat state tracks initiative, turn order, HP changes, and conditions."
    - "Combat actions still resolve through deterministic rules execution."
    - "The gameplay loop exposes enough state for live Discord play."
---

<objective>
Build the combat loop on top of the deterministic rules foundation so a live session can run initiative and turn-based resolution safely.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add combat state and initiative-driven turn loop</name>
  <files>src/dm_bot/gameplay/combat.py, src/dm_bot/rules/actions.py, src/dm_bot/rules/engine.py, tests/test_combat_loop.py</files>
  <action>Implement combat encounter state with initiative order, active turn tracking, HP updates, simple conditions, and deterministic attack/damage flow. Extend the rules layer only where needed for combat loop coverage.</action>
  <verify>
    <automated>uv run pytest tests/test_combat_loop.py -q</automated>
  </verify>
  <done>The runtime can run a basic heavy-rules combat loop with deterministic state changes.</done>
</task>
</tasks>
