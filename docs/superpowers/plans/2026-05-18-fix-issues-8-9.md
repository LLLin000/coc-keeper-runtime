# Fix Issues #8 and #9 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve GitHub issues #8 (licensing/trademark) and #9 (hardcoded absolute paths) to make the project safe for open-source collaboration.

**Architecture:** This is a cleanup pass — deleting Windows batch files, generalizing personal paths in AGENTS.md, adding MIT LICENSE, and removing "Call of Cthulhu" trademark from all public-facing docs and code.

**Tech Stack:** Git, Markdown, Python docstrings

---

## Pre-work: State Check

Current branch: `codex/architecture-refactor` (5 commits ahead of `master`)
Working tree: `restart-system.cmd` already `git rm`'d, `run-bot-bg.cmd` already deleted.
README.md has uncommitted changes (rewrite in progress).

---

## Task 1: AGENTS.md - Generalize Hardcoded GSD Paths (#9)

**Files:**
- Modify: `AGENTS.md` (lines with `C:\Users\Lin\.config\opencode\get-shit-done`)

- [ ] **Step 1: Read current AGENTS.md to confirm exact lines**

Read: `AGENTS.md` lines 1-30, 320-360

- [ ] **Step 2: Replace all `C:\Users\Lin\.config\opencode\get-shit-done` with `~/.config/opencode/get-shit-done`**

There are ~8 occurrences across lines 15, 32, 331, 343-347. Use `replaceAll` on AGENTS.md:
```
old: C:\Users\Lin\.config\opencode\get-shit-done
new: ~/.config/opencode/get-shit-done
```

- [ ] **Step 3: Verify no remaining `C:\Users\Lin` in AGENTS.md**

Run: `rg "C:\\\\Users\\\\Lin" AGENTS.md`
Expected: no output (or only in comments explaining the old path if we choose to keep a note)

- [ ] **Step 4: Commit**

```bash
git add AGENTS.md
git commit -m "chore: generalize GSD install paths in AGENTS.md (#9)"
```

---

## Task 2: AGENTS.md - Remove "Call of Cthulhu" Trademark (#8)

**Files:**
- Modify: `AGENTS.md` (Project Overview section, lines ~414-465)

- [ ] **Step 1: Replace project description line 417**

```
old: **Discord AI Keeper** — 本地模型驱动的 Call of Cthulhu 跑团系统
new: **Discord AI Keeper** — 本地模型驱动的 horror investigation TTRPG 跑团系统
```

- [ ] **Step 2: Replace Core Value line 427**

```
old: Run real multiplayer Call of Cthulhu sessions in Discord where a local AI Keeper can narrate, roleplay NPCs, and enforce COC rules without constant manual bookkeeping.
new: Run real multiplayer horror investigation sessions in Discord where a local AI Keeper can narrate, roleplay NPCs, and enforce d100 rules without constant manual bookkeeping.
```

- [ ] **Step 3: Replace Rules Scope line 435**

```
old: - **Rules Scope:** COC rules support is required in v1
new: - **Rules Scope:** d100 investigation rules support is required in v1
```

- [ ] **Step 4: Replace Architecture line 464**

```
old: - **Rules layer** — deterministic COC dice, success grades, SAN, combat and checks
new: - **Rules layer** — deterministic d100 dice, success grades, sanity, combat and checks
```

- [ ] **Step 5: Verify grep for remaining trademark**

Run: `rg -i "call of cthulhu" AGENTS.md`
Expected: no output

- [ ] **Step 6: Commit**

```bash
git add AGENTS.md
git commit -m "docs: remove Call of Cthulhu trademark from AGENTS.md (#8)"
```

---

## Task 3: README.md - Remove "Call of Cthulhu" Trademark (#8)

**Files:**
- Modify: `README.md` (current version already has uncommitted changes)

- [ ] **Step 1: Read current README.md state**

Read: `README.md`

- [ ] **Step 2: Replace line 3 trademark reference**

```
old: 本地模型驱动的 **Call of Cthulhu 7th Edition** 跑团系统。多个真人玩家在 Discord 里跑团，AI 扮演 Keeper 叙事、NPC 扮演、规则结算。
new: 本地模型驱动的 **d100 horror investigation** 跑团系统。多个真人玩家在 Discord 里跑团，AI 扮演 Keeper 叙事、NPC 扮演、规则结算。
```

- [ ] **Step 3: Replace any remaining "COC" or "Call of Cthulhu" in public-facing text**

Run: `rg -i "call of cthulhu|coc " README.md` to check
Replace:
  - "COC 规则" → "d100 rules" or "horror investigation rules"
  - "COC7e 完整规则" → "d100 investigation rules"
  - "COC7e 规则" → "d100 investigation rules"

- [ ] **Step 4: Verify no remaining trademark**

Run: `rg -i "call of cthulhu" README.md`
Expected: no output

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: remove Call of Cthulhu trademark from README.md (#8)"
```

---

## Task 4: Remove Trademark from Python Source Files (#8)

**Files:**
- Modify: `src/dm_bot/rules/coc/derived.py:12`
- Modify: `src/dm_bot/narrator/prompts.py:6`

- [ ] **Step 1: Remove rulebook reference from derived.py**

```
old: Reference: Call of Cthulhu 7th Edition Keeper's Rulebook, Chapter 2
new: Reference: d100 horror investigation rules, Chapter 2 — Derived Attributes
```

- [ ] **Step 2: Genericize narrator prompt**

```
old: f"你是克苏鲁的呼唤（Call of Cthulhu）的守秘人（Keeper）。\n"
new: f"你是一个 horror investigation TRPG 的守秘人（Keeper）。\n"
```

- [ ] **Step 3: Verify tests still pass**

Run: `uv run pytest -q`
Expected: 49 passed

- [ ] **Step 4: Commit**

```bash
git add src/dm_bot/rules/coc/derived.py src/dm_bot/narrator/prompts.py
git commit -m "chore: remove Call of Cthulhu references from source code (#8)"
```

---

## Task 5: Add MIT LICENSE File (#8)

**Files:**
- Create: `LICENSE`

- [ ] **Step 1: Create LICENSE file with MIT license**

Write standard MIT license with `Copyright (c) 2026 LLLin000`

MIT template:
```
MIT License

Copyright (c) 2026 LLLin000

Permission is hereby granted, free of charge, to any person obtaining a copy...
(standard MIT text)
```

- [ ] **Step 2: Commit**

```bash
git add LICENSE
git commit -m "docs: add MIT license (#8)"
```

---

## Task 6: Delete Old Batch Files (#9)

(Already partially done: `restart-system.cmd` git rm'd, `run-bot-bg.cmd` deleted)

- [ ] **Step 1: Verify both files are gone**

Run: `Test-Path restart-system.cmd; Test-Path run-bot-bg.cmd`
Expected: False, False

- [ ] **Step 2: Commit if not already committed**

```bash
git status
git commit -m "chore: remove Windows batch files with hardcoded paths (#9)"
```

---

## Task 7: Clean Up Planning State

**Files:**
- Modify: `.planning/PROJECT.md` — update trademark references, match current 7-package architecture
- Delete: `.planning/codebase/` old deleted files from staging (already staged for deletion)

- [ ] **Step 1: Stage new codebase docs and commit pending .planning/ changes**

```bash
git add .planning/codebase/
git status
```

- [ ] **Step 2: Update PROJECT.md trademark references**

Same pattern: "Call of Cthulhu" → "horror investigation", "COC" → "d100"

- [ ] **Step 3: Commit**

```bash
git add .planning/
git commit -m "chore: update planning state for architecture refactor"
```

---

## Task 8: Final Verification Gate

- [ ] **Step 1: Run pytest**

Run: `uv run pytest -q`
Expected: 49 passed

- [ ] **Step 2: Run smoke-check**

Run: `uv run python -m dm_bot.main smoke-check`
Expected: "All core modules import successfully."

- [ ] **Step 3: Verify no hardcoded paths remain**

Run: `rg "C:\\\\Users\\\\Lin" --include='*.md' --include='*.py' .planning/archives/` → archives are OK
Run: `rg "C:\\\\Users\\\\Lin" AGENTS.md README.md src/` → expected: 0

- [ ] **Step 4: Verify no trademark in public docs**

Run: `rg -i "call of cthulhu" README.md AGENTS.md LICENSE src/dm_bot/narrator/ src/dm_bot/rules/coc/derived.py`
Expected: 0

- [ ] **Step 5: Merge refactor to master**

```bash
git checkout master
git merge codex/architecture-refactor
```

---

## Task 9: Relocate Project

- [ ] **Step 1: Create target directory**

```powershell
New-Item -ItemType Directory -Path "C:\Users\Lin\Projects\discord-ai-keeper" -Force
```

- [ ] **Step 2: Move all files**

```powershell
Get-ChildItem -LiteralPath "C:\Users\Lin\Documents\Playground" -Exclude ".venv", ".pytest_cache", ".ruff_cache" | Move-Item -Destination "C:\Users\Lin\Projects\discord-ai-keeper" -Force
```

- [ ] **Step 3: Recreate venv**

```powershell
Set-Location "C:\Users\Lin\Projects\discord-ai-keeper"
uv sync
```

- [ ] **Step 4: Final verification**

```bash
uv run pytest -q
uv run python -m dm_bot.main smoke-check
```

Expected: 49 passed, smoke-check OK

---

## Summary

| Task | Target Issue | Risk |
|------|-------------|------|
| 1. AGENTS.md paths | #9 | None — text replacement |
| 2. AGENTS.md trademark | #8 | None — text replacement |
| 3. README.md trademark | #8 | None — text replacement |
| 4. Python source trademark | #8 | Low — prompt text change only |
| 5. MIT LICENSE | #8 | None — new file |
| 6. Delete batch files | #9 | Already done |
| 7. Planning state | both | Low — doc updates |
| 8. Verification gate | both | Must pass |
| 9. Relocate | both | Medium — venv rebuild, verify paths |
